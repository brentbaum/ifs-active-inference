# V2.3.2 formation re-foundation — frozen analysis plan

Frozen before any schedule run. All seeds are below 800000. T/D/P is scored
as a three-class finite comparison; P alone is identity formation.

## Gate 1

All candidate rows normalize within `1e-12`. No-event and masked incremental
BFs are exactly zero within `1e-12`. Published factor terms recombine within
`1e-10`. Overwhelm must increase the expected discriminating BF magnitude;
low versus high control must change outcome evidence in the preregistered
direction; integrated versus collapsed broadcast must change localization
evidence without a direct P increment. The analytic maximum one-slice
pairwise log BF must bound every enumerated row. The independent scorer must
agree within `1e-10`. All nine constitutional invariants pass.

## Gate 2

Seeds `750000:750299`, 100 worlds generated from each candidate, 24 event
slices per world. Frozen criteria:

- overall candidate accuracy `>= .80`;
- multiclass Brier `<= .15`;
- ten-bin confidence ECE `<= .08`;
- each diagonal confusion rate `>= .75`;
- D↔P confusion rate `<= .15`;
- false-P rate in no-event and high-control T worlds `<= .05`;
- generating row-parameter MAE `<= .10`;
- nominal 95% candidate coverage `>= .90`.

## Gate 3

Seeds `751000:751299`. Report acute, gradual, overwhelm-with-control,
low-control-without-overwhelm, broadcast contrast, real-danger, no-event
lengths 16/64/80/160, and matched-statistic permutations.

Primary generalization criterion: histories with identical multisets of
public sufficient configurations and observations must have maximum
pairwise final-log-evidence difference `<= 1e-10` across ordering,
clustering, regularity, and padded no-event length. Every no-event endpoint
equals its prior within `1e-12`.

Profile criteria: real-danger worlds choose D over P by mean margin
`>= .20`; low-control collapsed overwhelming worlds choose P over T by mean
margin `>= .20`; high-control integrated worlds have false-P rate `<= .05`;
overwhelm sharpening and broadcast localization paired effects have 95%
interval lower bounds above zero. V2.1 and V2.2.1 composition gates pass.

## Gate 4

Run the five contract lesions separately. Each targeted contrast must fall
below one quarter of intact magnitude or inside `±.02`. Required survivors:
D under root lesion; outcome evidence under context lesion; localization
under control lesion; ordinary evidence under precision lesion. Every
standing V2.0–V2.2.1 gate and constitution audit survives.

## Gate 5

Rerun all V2.0, V2.1, and V2.2.1 gates plus the permanent constitution.
Repeat the full blocks deterministically. Sweep candidate priors by ±20%,
predictive probabilities and complexity jointly by ±10%, and all nuisance
dimensions. Signs and schedule invariance must survive. Every failed world,
if any, is retained with full per-channel and normalization BF decomposition.

The historical V2.3/V2.3.1 failures, corrected v2.3.1r ledger, and shelved
attribution Gate-2 failures remain unchanged.
