# V2.1 frozen analysis plan

Frozen before execution: 2026-07-27.

- Gate 1: high precision must increase categorical correct-class probability
  by `>= .15`; analytic and numerical likelihoods agree `< 1e-12`; Gaussian
  variance must decrease monotonically.
- Gate 2: precision-state recovery accuracy `>= .70`; monitor-reliability
  posterior MAE `<= .08` and 95% interval coverage `>= .85`; calibrated
  observations must favor higher precision and unreliable observations lower.
- Gate 3: broadcast-on depth exceeds off by `>= .20`; changing inferred Phi
  changes the log odds delivered to another latent by `>= .20`.
- Gate 4: deleting only broadcast reduces depth effect below `.05`, preserves
  local posterior within `1e-10`, and every V2.0 gate remains passed.
- Gate 5: all V2.0 tests pass on the extended package.

Open assays preregister reliable/unreliable, broadcast on/off, conflict,
unlabeled four regimes, and a capacity-matched independent-local comparator.
Report paired effects and 95% deterministic bootstrap intervals.
