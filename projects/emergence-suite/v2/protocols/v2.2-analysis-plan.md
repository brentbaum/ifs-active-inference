# V2.2 frozen analysis plan

Frozen before execution: 2026-07-27.

- Gate 1: true structure recovery accuracy `>= .80` over 64 seeds and mean
  posterior probability on the true structure `>= .70`.
- Gate 2: learned association posterior-mean absolute error `<= .10` with 95%
  interval coverage `>= .85`.
- Gate 3: identical cue correction changes treated M in every precision regime
  by `>= .20`; broad root uptake exceeds broadcast-off by `>= .08`, which
  exceeds narrowed by `>= .03`; broad untreated transfer exceeds narrowed by
  `>= .08`. Conditional on fixing G to its pre-correction distribution,
  transfer is `< 1e-10`.
- The 2x2 main association effect on transfer must exceed `.10`; perceptual
  similarity main effect magnitude must be `< .03`.
- Gate 4: deleting the cue-root association removes transfer below `.01` while
  preserving treated cue uptake `>= .20`; broadcast deletion preserves local
  fluency while reducing depth, root uptake, and transfer.
- Gate 5: all V2.0 and V2.1 tests pass unchanged.

Report mean effects and deterministic 95% bootstrap intervals; retain failures.

