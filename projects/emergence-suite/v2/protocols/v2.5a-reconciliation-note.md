# V2.5a reconciliation note (evaluator, 2026-07-29)

Two V2.5a documents coexist and this note binds them.

1. **Evaluator-authored format core** (`contracts/v2.5a-evidence-format-addendum.md`, `protocols/v2.5a-analysis-plan.md`, committed 678d1dc, before the master spec landed): presentation operator, derived candidates, episodic increment ΔI, information matching m*/n, formed-P bridge. In flight — pilot consumed 755000:755199, SESOIs frozen (d9f5292), gate 1 pass (1284893), gate 2 pass on 756000:756399 (e2856dd).
2. **Master-spec V2.5a** (`projects/ifs-paper/suite-v2-remaining-stages-spec.md` §3, GPT-5.6 Pro, adopted a4c7c22): adds exact spike-and-slab configural coupling as latent structure, joint/marginal forms with identical atomic evidence, ROPE-resolved format effects (positive OR equivalent both count as resolution — a null narrows the theory), 16 semantic proofs, Epoch-B seeds (gate2 1020000:1020799 …), sealed C-V25A on escrow 2010000:2010999.

## Binding resolution
- The format core is a **compatible subset**: the master spec's evidence-matching machinery (§3.2) is the format core's presentation/matching operators; its "identical atomic evidence" requirement is the format core's factorization identity. Nothing in the completed gates contradicts the master spec.
- The in-flight format-core arc **completes on its committed Epoch-A blocks** (757000:763999 as assigned in `v2.5a-parameters.json`) — those assignments predate the master spec and are already partially consumed; switching mid-arc would violate the plan-before-execution discipline. All those blocks are then barred.
- The master spec's additional V2.5a structure (spike-and-slab configural coupling and its estimands, ROPE resolution rule, remaining proofs) is implemented as the **stage completion pass on Epoch-B blocks** per the seed map, after R0's grammar exists (R0 runs in parallel on branch r0-grammar).
- **C-V25A is sealed only after R0 passes its gates and the four-layer linter is mechanized** — the R0-first rule from the master spec governs all seals. No seal for the format core alone.
- The stage verdict is single: V2.5a concludes only when both the format core and the master-spec additions pass, with one cumulative freeze. Epoch-A consumption is disclosed in the freeze report.

## Parallel-track stop discipline (standing)
- Each track (V2.5a science; R0 apparatus) stops honestly and independently at blocking failures; pure software errors stop only their own track (invalidate-and-repeat with byte identity).
- Criterion-validity or representational-absence findings halt **all** tracks pending external adjudication.
- R0 may not modify any frozen file; V2.5a may not touch generators. Merge order: whichever track freezes first merges first; the second rebases and re-verifies byte identity of the first's frozen artifacts.
