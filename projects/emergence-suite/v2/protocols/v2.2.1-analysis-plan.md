# V2.2.1 frozen analysis plan

Frozen after the prerepair diagnosis and before repaired protocol execution:
2026-07-27.

- Gate 1: analytic spike-and-slab posterior and an exact finite factor model
  must agree within `1e-10`. At history length 180, mean posterior probability
  on the true existence component must be `>= .90` for both true-zero and
  true-associated (`theta=.9`) worlds. Whole-structure recovery remains
  accuracy `>= .80` and mean true probability `>= .70`.
- Gate 2: associated-slab posterior-mean MAE `<= .10` with 95% interval
  coverage `>= .85`; cue-existence classification accuracy `>= .90`.
- Gate 3: every original V2.2 seam threshold remains in force. In 256 new
  open true-zero worlds at n=180, at least 95% must keep untreated transfer
  within `.02`; true-associated treatment must retain mean transfer `>= .15`.
- Gate 4: fixing posterior existence to the exact-zero component removes
  transfer below `.01` while treated-cue uptake remains `>= .20`; fixed-G
  mediation remains below `1e-10`; broadcast-off dissociations survive.
- Gate 5: rerun all V2.0, V2.1, and V2.2 open gates. Every inherited gate must
  pass unchanged. The repair must not modify inherited parameter blocks.

All effects use paired means with 95% intervals. Failures are retained
verbatim. Development seed blocks remain below 800000.

