# V2.G0 executable analysis plan

**Stage:** R0 compositional world/protocol grammar  
**Status:** public apparatus plan  
**No scientific claim:** all criteria concern construction, exact probability,
schema, selectivity, regression, or custody.

## Frozen boundary and stop rule

V2.G0 adds new files only. Any need to edit a standing V2.0–V2.4.4 or
V2.5a-in-flight file is a blocking representational conflict: stop and write
`results/R0/diagnosis-stub.md`. At the first blocking gate failure, record the
observed result unchanged and stop before the next seed block.

The C-V2G0 escrow `2000000:2000499` is evaluator custody and is not accessed by
this plan.

## Gate 1 — semantic proofs

Run `tests/test_v2g0_grammar.py`. The 24 named proofs cover:

1. normalization and production/oracle parity for all eleven primitives;
2. exact onset-window mass;
3. exact recurrence-conditioned mass;
4. subset scope;
5. disjoint product;
6. shared latent;
7. explicit finite mixture;
8. mixed drift plus recurrent context;
9. action intervention;
10. candidate-common missingness;
11. bridge family parameterization;
12. independent world-probability parity;
13. protocol-label exclusion;
14. four-part RNG keys;
15. escrow refusal;
16. diagnosis-block refusal;
17. score-free deterministic dry-run;
18. overlap rejection;
19. transition normalization rejection;
20. bounded-continuous density;
21. minimum visits;
22. no rejection sampling;
23. trace custody fields;
24. joint-policy outcome conditioning.

All 24 must pass.

## Gate 2 — generator recovery

Use every seed in `1000000:1000999`, assigning 100 consecutive seeds to each
of ten public fixture families: iid, Markov, ordered drift, constrained change
point, recurrent context, action contingent, masked observation, joint
episode, partner process, and mixture.

Recover public kind, scope, onset support, recurrence property, selected
mixture component, exact schema/support, and exact normalized probability from
the trace. Required:

- exact schema/support recovery;
- family and scope accuracy at least `.95`;
- maximum production/oracle log-probability discrepancy at most `1e-10`;
- zero out-of-support worlds.

## Gate 3 — composition battery

Use every seed in `1001000:1002999`, round-robin over six frozen public cells:

- subset drift;
- constrained change point;
- recurrence-guaranteed context split;
- mixed subset drift plus recurrent split;
- family-parameterized bridge from a banked state;
- partner switch plus action-contingent availability.

Each cell compiles and executes through the same public API with no cell branch
in compiler, sampler, protocol runner, or bridge. For each cell, two initial
seeds also receive the score-free four-layer schema dry-run.

## Gate 4 — selective mutations

Use every seed in `1003000:1003999`. Pair a two-process baseline and a variant
under the same seed. Alternate:

- mutate only the target process distribution;
- mutate only the target process scope.

The unrelated process value, its component RNG keys, and a canonical inherited
V2.4 scientific posterior byte string must be identical. Only the world hash,
declared target fields, relevant scope/schema metadata, and resulting exact
world probability may differ.

## Gate 5 — cumulative regression

Use every seed in `1004000:1009999` for a deterministic R0 cumulative sampling
audit. Then run:

```text
python3 -m unittest discover -s tests -v
```

This is the executable standing V2.0–V2.4.4, V2.5a-in-flight, and V2.G0 unit
suite. Verify every V2.4.4 freeze-manifest hash, and verify that no tracked
pre-R0 file differs from `HEAD`. Any mismatch blocks.

## Four-layer pre-seal linter

Every future cell row must include:

- public obligation;
- world constructor call and declared domains;
- protocol constructor call;
- composition operation;
- bridge path and family parameter;
- conditioning declaration and exact normalizer where applicable;
- requested trace fields;
- dry-runs on at least two public development seeds;
- `scientific_scores_inspected=False`;
- `new_code_required=False`.

The blocking question is: **Can the exact sealed population be generated and
scored after reveal with zero source-code change?**

