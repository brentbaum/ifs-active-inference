# V2.G0 public dummy bundle

The executable public constructors are in `ref/v2g0_fixtures.py`.
`cells.json` freezes the public Gate-3 cell names, constructor mapping,
composition obligation, and requested schema without consuming any sealed
challenge design or seed.

