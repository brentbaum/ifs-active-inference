# V2.3.3 bank2 public sampling plan (sampling-adequacy requalification)

**Public, frozen before the bank2 seal. Everything scientific is inherited unchanged from the frozen V2.3.3 stage (3e9bad2 + the seed-authorization repair addendum): same constructor, same eligibility rules, same three q0(P) bands (0.60–0.75 / 0.75–0.90 / 0.90–0.98), same upper-saturation exclusion, same first-40-per-band retention, same 120-state serialized bank schema.**

- **Candidate block:** one fresh contiguous evaluator-escrowed block of exactly **5,504** candidate seeds, consumed once, in ascending order, in full — no early stopping when quotas fill. Rationale: N_min ≈ 5,365 for ≥ 99% probability of ≥ 40 moderate states at the observed Wilson lower bound (0.01045); 5,504 exceeds it and is divisible by 8, balancing the frozen eight-element history-length cycle. Expected moderate yield ≈ 96 at the point estimate, ≈ 58 at the lower bound.
- **Interpretation lock:** this is not a replication or reversal of the 800-seed natural-yield result (which stands as the prospective yield finding). It tests only whether a rate-powered fresh block can assemble the unchanged stress bank the frozen maintenance analysis requires.
- **Descriptive census (non-criterial):** q(P) histogram/ECDF; log-evidence-margin m0 histogram/ECDF; yield by history length and sufficient-statistic profile; fill curves; upper-saturation rate; first-crossing and post-crossing evidence summaries; comparison with the 800-seed census (comparison descriptive; the bank2 verdict belongs to the fresh block alone — no pooling for the verdict).
- **Stop rule:** one retry only. Pass → the existing sealed maintenance bundle's seeds are released. Fail → equal-band design retired; C-V233-M archived unopened; V2.3.3b (formed-state bank, log-evidence moderator, new public plan, fresh seal) is designed instead.
