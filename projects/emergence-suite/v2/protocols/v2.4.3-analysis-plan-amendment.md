# V2.4.3 analysis-plan amendment (executable)

**Amends the frozen V2.4 plan (+ v2.4.1/v2.4.2 amendments) per the round-3 adjudication (`results/V2.4.2/gate3-adjudication.md`). Construct text: `contracts/v2.4.3-structural-existence-addendum.md`. Numeric freeze: `protocols/v2.4.3-parameters.json`. Committed before implementation; no V2.4.3 seed opened before this commit. No family likelihood, transition, parameter prior, candidate prior, common emission, bridge equation, or root-transfer mechanism changes.**

## Gate 1 — 20 semantic proofs
Retain the existing 16. Add:
17. **Exact path-class partition** — every CS path belongs to exactly one of Z=0/Z=1; prior masses sum to one; posterior masses sum to one; conditional path-class evidences recombine to full CS evidence within 1e-10.
18. **Structural-existence odds identity** — posterior/prior odds reproduce BF_2C:1C; independent enumeration matches path-class masses and BF within 1e-10; one-context and recurrent-two-context public dummies have the frozen expected directions.
19. **Bayesian model-average identity** — held-out BMA sequence probability equals direct log-sum-exp of pre-held-out family weights and candidate sequence probabilities; no held-out token changes pre-held-out weights, coordinates, matching, or material-split classification; an independently authored mixture calculation agrees within 1e-10.
20. **Readout purity** — material_redescription, Z_2C, path-class BF, BMA score, regret, family argmax, and raw control label are pure outputs; no new assignment target or branch enters inference; all one-posterior and evidence constitutions remain exact.

## Gate 2 — five-family recovery
Fresh seeds **787000:787499**, 100 worlds per family, 96 slices. Retain every V2.4.2 threshold: diagonal and macro recovery >= 0.60; Brier <= 0.15; ECE <= 0.08; posterior-set coverage >= 0.90; parameter coverage/MAE unchanged; raw false CS <= 0.10 separately in DR and CP. Add: material-redescription rate >= 0.60 in CS worlds; <= 0.10 separately in GW, CL, DR, CP worlds; complete path-class confusion and BF distributions. No old gate-2 seed reused for the new stage verdict.

## Gate 3 — open scientific assays

| Assay | Fresh seeds | Design |
|---|---|---|
| 1–2 DR/CP false-split controls | 787500:787739 | 120 DR + 120 CP, 96 slices |
| 3 Exact-family held-out/BMA | 787800:788199 | 80 per family, 32 slices |
| 5 Misspecification | 788200:788439 | 240 worlds |
| 6 Genuine then/now + paired nulls | 788500:788619 | 120 paired worlds |
| 7 Dedicated cue-local control | 788620:788739 | 120 CL worlds, 96 slices |
| 8 Formed-P bridge | 788800:788919 | 120 frozen bank states |

Guard gaps between blocks are intentional.

- **Assays 1–2 (DR/CP):** legacy raw CS ceiling <= 0.10; material-redescription ceiling <= 0.10; recovery, held-out scores, path classes, and complexity reported.
- **Assay 3 (predictive adequacy):** BMA generator-family regret upper 95% bound <= 0.01 nats/token, separately for all five families; CS matched count >= 60/80; CS matched margin >= 0.01, lower CI > 0; every other point-family margin descriptive.
- **Assay 4 (decomposition):** exact recombination of candidate evidence, parameter complexity, path complexity, CS one-/two-context evidence, and BMA score; max error <= 1e-10.
- **Assay 5 (misspecification):** descriptive — BMA regret to hindsight best; posterior entropy; family trajectory; material-split support; candidate decompositions. No invented true-family label.
- **Assay 6 (genuine then/now):** retain all current transfer and retention criteria, plus raw CS selection >= 0.60; material-redescription rate >= 0.60; mean split log BF lower CI > 0; CS matched held-out margin >= 0.01, lower CI > 0; present indexing and root-mediated transfer unchanged.
- **Assay 7 (controls):** conditional-product and single-regime, separately: material-redescription rate <= 0.10; raw CS rate descriptive; path/complexity decomposition mandatory. Dedicated CL worlds (96 slices): CL recovery >= 0.60; material redescription <= 0.10; untreated cues unchanged except through the inherited root. Root-clamped and zero-association transfer remain exact zero controls.
- **Assay 8 (formed-P bridge):** genuine arm — material-redescription rate >= 0.60; CS held-out margin and historical-retention criteria unchanged; present-context root-mediated transfer >= 0.05, lower CI > 0; G-fixed transfer exactly zero. Shuffled and single-regime arms — material-redescription rate <= 0.10 separately; raw CS selection descriptive. Balanced by bank stratum; no claim that q(P) changes family priors.

## Gate 4 — lesions
Fresh seeds **789000:789239**. Retain all five lesions with the new target mapping — a lesion is not successful merely because raw CS argmax changes:

| Lesion | Required effect |
|---|---|
| CS transition learning fixed | transition recovery and return prediction weaken; path-class evidence reported |
| marker–meaning joint → marginal product | material redescription falls to <= 0.10; present indexing disappears |
| cue-root association zeroed | transfer disappears; material split may survive |
| global broadcast severed | root uptake/transfer disappear; family and path-class evidence survive |
| temporal families equalized | raw recovery, material split, and all family BFs disappear |

## Gate 5 — cumulative regression and robustness
Fresh seeds **789300:790299**. Run every standing V2.0–V2.3.3 gate; both constitutions; V2.4.3 gates 1–4; lengths 16/32/64/96; all existing prior, cue-count, reliability, missingness, transition and root-strength sweeps; path-class support and BMA regret in every cell. Primary-cell thresholds must pass; non-primary robustness cells report effects and failure localizations without pooling.

## Freeze and Gate 6
1. Freeze source, parameters, plans, path-class tests, BMA tests, exact oracles, and the compatibility attestation. 2. Evaluator verifies hashes and all seed boundaries. 3. Reveal C-V24 (hash 574131ce…) with hash check. 4. Validate it through the unchanged public API without opening seeds. 5. Release only the sealed 830001:830600 block. 6. Run once. 7. Seal raw traces before evaluation. 8. Report the immutable challenge verdict first, then scientific/semantic/stress/custody classes. The public plan's prospection-failure stop applies verbatim.

## Stop rule
No automatic V2.4.4 (see adjudication). Numeric misses do not justify adjusting 0.80, 4.0, 0.60, 0.10, or 0.01.
