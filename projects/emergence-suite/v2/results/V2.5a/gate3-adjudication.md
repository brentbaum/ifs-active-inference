# V2.5a format-core gate-3 adjudication (evaluator, 2026-07-29)

Per the standing repair/adjudication taxonomy (master spec §1.10) and the user's self-execution directive; to be disclosed at the next external consultation round. The gate-3 verdict remains **FAIL as recorded**. The three blocking failures receive distinct classifications:

## 1. Dose-monotone m*/n criterion — criterion-authoring defect (retired)
The diagnosis proves analytically that the frozen association-dose operator permutes markers only and never enters the root-channel matching posterior; m*(a) = m* identically, and the assay's unpaired-seed design guarantees any observed group differences are root-path sampling noise. The criterion cannot measure its named construct. Under the 7-item adjudication-criterion audit it fails items 2 (construct interpretation), 4 (attainable behavior never evaluated against the pathway), and 5 (contradicts the frozen interface's algebra). **Retired without replacement**; m*'s actual determinants (missingness before crossing r=0.75, opposing token pairs r=0.93, root switches r=0.87 — first-passage structure of the root-token walk) are reported descriptively. The evaluator (Fable) authored this criterion; the defect is the evaluator's.

## 2. Matching tolerance .01 on the formed-bank population — criterion-authoring defect (retained as limitation)
All 120 scans were uncensored; the 17 failures are off-lattice target crossings (KL jump at the crossing r=0.92 with error; no within-tolerance prefix exists anywhere through the 8n cap). The tolerance was frozen from a pilot population (fixed 0.85 CPT, prior 0.5/0.5) whose target sat 0.0077 off the lattice by construction; the banked-bridge population's targets (arbitrary banked priors × bank-specific association reliability) are generally not lattice points, and low-reliability states produce coarse crossings. The criterion as authored conflates scan granularity with matching failure. **The literal FAIL is retained as a limitation**; the bridge contrast is reported descriptively for the 103 within-tolerance worlds and not reported as "information-matched" for the 17. No post-hoc tolerance change. The master-spec V2.5a completion pass must define matching prospectively with a lattice-aware criterion (e.g., nearest-grid-point error) that passes the full 7-item audit including granularity analysis on the actual target population.

## 3. Decomposition identity — pure software error (repair authorized)
The authored unequal-length telescoping identity is valid (constant-tail convention; verified at error 0.0 on all 120 worlds when endpoints use one likelihood). The failure is an implementation inconsistency: the increment trajectory calls the fixed-0.85 `root_posterior` while the published endpoint uses the bank-reliability composition updater; the residual equals the endpoint-likelihood mismatch exactly in every world (seed 758569: 0.2471 accounted to the 0.85-vs-0.636 mismatch, residual 0.0). **Invalidate-and-repeat authorized, narrowly**: the increment trajectory must use the same bank-reliability root likelihood as the contract-facing endpoint; nothing else changes; original record retained; repaired execution recorded separately with the identity holding 120/120 within 1e-10; regression test pinning endpoint-likelihood consistency; byte identity on all non-decomposition quantities.

## Format-core disposition
With #1 and #2 classified as evaluator criterion-authoring defects and #3 repaired, the format core concludes as an **adjudicated mixed result**: the machinery identities are established (gates 1–2: factorization at 1e-14, increment identity, both-accounting constitution, ΔI 0.063/token vs .01 SESOI), assay-1 ΔI dose-response passed, and the matching/decomposition apparatus works with its limitations named. Gates 4–5 of the format-core arc proceed under this adjudication with #1 retired and #2 non-blocking-as-limitation; every other criterion remains blocking. The stage verdict for V2.5a overall remains OPEN pending the master-spec completion pass (post-R0), which must author its matching and dose criteria prospectively under the full audit. Epoch-A blocks consumed by this arc are disclosed and barred.
