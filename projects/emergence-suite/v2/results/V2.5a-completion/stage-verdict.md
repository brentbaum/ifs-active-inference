# V2.5a single stage verdict

**Stage disposition: `PASS_WITH_ADJUDICATED_FORMAT_CORE_LIMITATIONS`**

Per `protocols/v2.5a-reconciliation-note.md`, V2.5a receives one disposition
from the format core, the master-spec completion pass, and C-V25A:

- **Format core:** `ADJUDICATED_MIXED`. Its exact presentation,
  factorization, increment, matching, and bridge machinery stands. The
  dose-monotone `m*/n` criterion remains retired as construct-invalid; the
  17-world matching-lattice limitation remains named; the repaired
  within-tolerance bridge contrast remains `0.063 [0.045, 0.084]`.
- **Master-spec completion:** PASS Gates 1–5. The original Gate-1
  verdict-encoding FAIL and original Gate-5 custody FAIL remain in the
  ledger beside their authorized repaired PASS executions.
- **C-V25A:** immutable sealed PASS, six of six criteria. Scientific,
  semantic, and custody classes all pass.

The sealed challenge found:

- coupled-support difference `0.4932`, 95% interval
  `[0.4880, 0.4984]`;
- held-out joint advantage `0.0484` nats per atomic token, 95% interval
  `[0.0448, 0.0520]`;
- false-coupled rate `0.048`;
- context-mediation and historical-retention errors exactly `0`;
- root and transfer joint-minus-marginal effects both resolved positive
  under the frozen ROPE rule;
- maximum semantic error `3.552713678800501e-14`.

The evaluator's disclosed pre-seal construction, without scoring, of seed
`2010000` is retained in the custody record. The block assignment predates
that event.

## Claim license

The positive completion and sealed results license a format-specific
structural claim within this exact construction: matched atomic evidence can
support configural structure and improve held-out prediction when presented
as a joint episode. They do not license a generic claim that episodic
evidence is always superior. The format-core null/limitation findings remain
part of the stage rather than being overwritten by the sealed PASS.
