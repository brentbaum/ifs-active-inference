# V2.4 final disposition (GPT-5.6 Pro round 8, 2026-07-29)

**Ruling: conclude V2.4 with C-V24's PROSPECTION_FAILURE — UNRUN as the final primary sealed-challenge record. No C-V24b. Proceed to V2.5a.**

The challenge verdict remains exactly as committed: zero worlds generated; zero scientific criteria evaluated; zero escrow seeds consumed; process custody passed; no substituted population and no challenge-specific code.

## Why the C-V232-F precedent does not apply
C-V232-F was an evaluator-authored criterion referencing a missing freeze quantity — correcting it let the same scientific construct be evaluated on the same frozen model. A C-V24 replacement restricted to existing constructors would instead test a *different and easier* scientific question (dropping the mixed drift-plus-recurrent-split world, the subset-specific drift, the constrained schedules, and the drift bridge control). That would be a new exact-family replication challenge, not a corrected execution. Critically, the mixed cell was publicly foreshadowed (the public plan named V2.4's sealed challenge as "partial drift plus an abrupt contextual transition"), and the frozen gate-6 plan explicitly required an inexpressible sealed configuration to stop as a prospection failure — which it did.

## Attribution: shared public-contract-to-frozen-API expressibility failure
Not a pure evaluator-authoring defect. Two components, either alone sufficient to preserve the sealed stop:
1. **Evaluator over-specification** (should have been caught before sealing): drift restricted to a cue subset; a change point guaranteed within the middle 60%; guaranteed old-context recurrence; a drift-family formed-state bridge.
2. **Genuine frozen-API limitation** (publicly foreshadowed but impossible to construct): the core mixed cell — partial drift plus recurrent context splitting; and the formed-state bridge hard-coded to context-split worlds rather than family-parameterized.

## Final disposition record
Stage status unchanged: `FROZEN_ADJUDICATED_MIXED_SELECTIVITY_POWER_AND_NULL_EXCESS_CRITERION_LIMITATIONS`.
Final challenge disposition: `PRIMARY_SEALED_CHALLENGE_PROSPECTION_FAILURE_UNRUN`.
No "Gate 6 PASS"; formal gate-2 and gate-3 FAILs unaltered. Machine record: `round8-disposition.json`.

## Escrow disposition
The entire block 830001:830600 is retired: `RETIRED_UNCONSUMED_AFTER_CHALLENGE_REVEAL`. Although no seed was accessed, the plaintext is public and the block was formally assigned to it; reusing a remainder would create custody ambiguity.

## Permanent pre-seal linter amendment (adopted)
Generator expressibility becomes a mandatory linter class for every future sealed challenge, distinguishing four layers: (1) inference expressibility; (2) world expressibility; (3) protocol expressibility; (4) composition expressibility. Every private cell requires a traceability row (public obligation clause; frozen constructor call; declared arguments/domains; composition operation; bridge path with family parameter; conditioning declaration; requested readouts as frozen fields; dry-run result on ≥2 public development seeds; scientific-scoring-during-dry-run = false; new-code-required = false). Dry-runs may inspect only construction success, schema, lengths, supports, and field presence — never family selection, evidence, margins, transfer, or any criterion statistic. No seed rejection or conditioning unless publicly exposed as a scientific generator operation. Mixed-process cells must invoke an actual public composition operator. Bridges must accept the world family via public parameter. Linter output and constructor-call hashes are committed before the challenge plaintext hash. Final blocking question: "Could the exact sealed population be generated and scored after reveal with zero source-code change?" — a "no" blocks sealing.

## Paper treatment
Main-text limitation (adopted):
> A sealed challenge intended to combine partial drift with recurrent context splitting stopped before seed release because the frozen world-generator and bridge APIs could not express the requested mixed processes. No challenge trajectory was generated and no scientific criterion was evaluated. The inference model's redescription results therefore apply to the implemented exact-family and misspecification protocols; prospective generalization to composed drift-plus-recurrent-context worlds remains untested.

Computational record reports: challenge hash; full revealed plaintext; all five expressibility failures; 87/87 frozen hashes verified; zero worlds/criteria/seeds; custody pass; no substituted generator; no new runner or model code.

Scientific meaning: not evidence against context-indexed redescription — evidence that **the inference side became more compositional than the protocol-generation side**. The frozen system compares/recovers five families, computes matched held-out predictions, infers material splitting, preserves historical context, and mediates present-context transfer; it cannot yet declaratively construct per-subset temporal processes, mixed DR+CS worlds, onset windows, recurrence-guaranteed processes, or a family-parameterized bridge. That is a useful boundary for V2.8.

Revised V2.4 claim (adopted): the strong within-suite result followed immediately by the prospective boundary — see the ruling text; never say C-V24 "failed the redescription theory," that the five sealed criteria failed, that the mixed model was evaluated, that the challenge was replaced or repaired, or that V2.4 passed its primary sealed challenge.

## Next-stage consequence
Proceed to V2.5a. Do not backport mixed-world constructors into V2.4. Carry forward permanently: (1) stage-level challenge traceability — every sealed cell maps to a frozen public constructor before seal; (2) before V2.8, the suite must earn a **declarative compositional world grammar** (different processes on disjoint latent/cue subsets, composed without challenge-specific code); a future challenge may then test the scientific idea behind C-V24 on that architecture with a new seal and new escrow — not named C-V24b, not presented as a rescue of V2.4. The original C-V24 is the final V2.4 challenge result.
