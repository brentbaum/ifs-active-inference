# Sealed challenge C-V232-F2 — continuity, the disputed clause alone

**Sealed by evaluator after the empirical-bound addendum was committed. Runs on the frozen c67e853 formation stage with zero new code. Tests exactly the clause C-V232-F could not evaluate; every other C-V232-F verdict stands as recorded and is not re-litigated here.**

## Configuration
100 worlds from the unopened escrow remainder, seeds 813301:813400, drawn from the same generator families as C-V232-F's mixed-provocation and D/P-discriminator cells (the high-provocation portion of the population — acute events present, controllability shifts, broadcast interruptions), regenerated fresh at these seeds.

## Test (single)
Across all slices of all 100 worlds, with the committed addendum values (empirical p99 = 0.3345519502357523):
- (a) The fraction of acute-event slices whose single-slice persistent-posterior change exceeds the empirical p99 is ≤ 1.5%.
- (b) No slice anywhere exceeds 1.75 × empirical p99 = 0.5854659129125665.
- (c) The frozen analytic bound holds everywhere (as in C-V232-F, re-asserted).

Pass = all three clauses. Report the full distribution (p50/p90/p99/max) with the exceedance count and per-seed localization of any violation.
