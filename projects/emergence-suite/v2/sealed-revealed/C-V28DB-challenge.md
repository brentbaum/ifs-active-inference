# Sealed challenge C-V28D-B — recurrence and durability (vocabulary-corrected sealing)

**Second sealing. The C-V28D STOP_AS_SEALED verdict is retained as written: the ordering criterion named a 'redescription' first-time absent from the frozen readout (evaluator authoring error; the linter dry-ran trajectories but did not validate ordering-criterion keys against the first_times vocabulary — that check is now performed and passes). This sealing changes ONLY the ordering criterion, now over the frozen keys, and moves to a fresh escrow prefix. All other criteria verbatim.**

## Bundle (parse instruction: ast.literal_eval on the exact bracketed text; arms run via run_trajectory(state, seed, protocol=arm, released_block=(2130600,2131199)))

{'title': 'recurrence and durability', 'escrow_block': '2130600:2131199 (fresh prefix of the assigned 2130000:2139999 block; the never-opened 2130000:2130599 prefix is closed)', 'stratum': 'chronic_one', 'n_seeds': 600, 'arms': ['full', 'premature_do_over'], 'pairing': 'SAME-SEED: each qualifying seed runs both arms', 'criteria': ['qualifying-world population (piloted eligible rate .74)', 'full arm: material_reduction >= .80 (pilot .93); followup_retention >= .99 (pilot 1.0); mean rupture_return in [.55, .90] (pilot .74) — old-context recurrence is handled, neither ignored nor globally re-current', 'premature_do_over arm: material_reduction <= .10 (pilot 0) — premature do-overs do not produce durable reduction; premature_return_reversal rate reported with its denominator (descriptive)', 'historical availability without global currentness: historical_index_available true AND historical_context_error <= 1e-10 in every world of both arms', "first_times ordering over the FROZEN keys (depth, policy, contact, root, reduction): in successful full-arm worlds, root revision precedes material reduction (times['root'] < times['reduction']); violation rate <= .05; full first_times distributions published"]}


Common custody: seeds consumed once ascending gap-free within the declared sub-block; each qualifying seed's protocol arms are declared same-seed pairs (one seed consumption, multiple protocol arms); raw traces sealed and hashed before criteria; population statistics use the standing whole-world bootstrap; verdict classes scientific/semantic/custody; software errors invalidate-and-repeat with byte identity; each bundle reports its own immutable verdict and no aggregate pass count is required (spec section 9).
