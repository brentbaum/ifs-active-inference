# Sealed challenge C-V28B — chronic, multiple protectors

**Sealed by the evaluator after the V2.8 open-gate freeze candidate (61aa91f; all open gates passed; 128 genomes and 3,072 profiles retained without selection). Pre-seal linter: composition dry-run of the full trajectory per stratum on development seeds through the exact frozen path (generate_developmental_state -> qualifies -> run_trajectory); protocol and stratum vocabulary validated against the frozen PROTOCOLS/STRATA; released_block threading verified; all floors derived from the committed stage-0 attainability pilot and gate-3 per-protocol profiles (barred blocks 1680000:1689999) with margins; same-seed pairing for every within-world contrast (the C-V27 lesson).**

## Bundle (parse instruction: ast.literal_eval on the exact bracketed text; arms run via run_trajectory(state, seed, protocol=arm, released_block=<escrow block>))

{'title': 'chronic, multiple protectors', 'escrow_block': '2110000:2110599 (remainder unconsumed, closed at verdict)', 'stratum': 'chronic_multiple', 'n_seeds': 600, 'arms': ['full', 'unreliable_partner'], 'pairing': 'SAME-SEED: each qualifying seed runs both arms', 'criteria': ['qualifying-world population (piloted eligible rate .78)', 'full arm: successful_sequence >= .70 (pilot .84); material_reduction >= .70 (pilot .84); mean protector_trust_update > 0 (pilot +.31); mean depth_increase >= .30 (pilot .41)', 'unreliable_partner arm: mean protector_trust_update < 0 (pilot -.49); mean depth_increase <= 0 (pilot -.12); successful_sequence <= .10 (pilot 0); paired full-minus-unreliable untreated_transfer difference lower 95% bound > .10 (pilot gap .20)', 'historical_context_error <= 1e-10 everywhere']}

Common custody: seeds consumed once ascending gap-free within the declared sub-block; each qualifying seed's protocol arms are declared same-seed pairs (one seed consumption, multiple protocol arms); raw traces sealed and hashed before criteria; population statistics use the standing whole-world bootstrap; verdict classes scientific/semantic/custody; software errors invalidate-and-repeat with byte identity; each bundle reports its own immutable verdict and no aggregate pass count is required (spec section 9).
