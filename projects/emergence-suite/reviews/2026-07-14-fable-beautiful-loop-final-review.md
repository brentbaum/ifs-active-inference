# Fable final review — Beautiful Loop experiments 38–42

**Date:** 2026-07-14

## Materials reviewed

Fable read the v11 manuscript and fidelity audit, experiment narratives 33–42,
the unified-relational and identifiable-globality implementations, and the raw
JSON outputs. It independently matched the manuscript's cited values to those
outputs and verified that the Experiment 42 freeze commit preceded its results
commit.

## Direct verdicts

- **Minimal precision hyper-loop: yes**, qualified as a faithful minimal
  implementation under direct layer monitoring, not the bottom-sensed Table 1
  observation graph.
- **Three-operation conjunction: yes**, qualified because binding and action
  are directly load-bearing, while precision recursion is directly ablated
  only along the action-selection path.
- **Revised claim in v11: yes**, conditional on sharper wording about the
  failed cutoff, partial identification, interaction headroom, and supplied
  precision basis.

Fable found no fatal issue. It judged the independent local control, exact
factorized replay, matched-budget random arm, and matched-marginal projection
fair and capacity-relevant. It agreed that Experiment 40's failed raw
zero-relation manipulation was confounded and that Experiment 42 confirmed
amplification rather than an all-or-none gate.

## Corrections made after review

Across the manuscript, audit, experiment record, and raw summary, the committed
record now:

- call the `0.015` miss one of six equally weighted frozen criteria rather than
  labeling it auxiliary after the fact;
- state that Experiment 42's overall frozen status is failed;
- report paired-seed 95% intervals for binding, relational action, factorized
  action, and their interaction;
- distinguish the roughly fivefold raw action-gain ratio from the roughly
  twofold contrast after descriptive headroom normalization;
- state that the unified agent's bottom-only observations identify total
  channel variance, not the nine layerwise components;
- narrow the demonstrated interaction directions to precision-to-action and
  relation-to-action-value;
- disclose that the supplied loading basis heavily scaffolds channel reversal;
  and
- state that no arm ablates precision inside perceptual inference.

## Remaining boundaries

The construction still uses authored relational and precision-loading
families, an expected-information-gain surrogate rather than exact expected
free energy under one continuous model, line-search-enforced descent, and a
linear-Gaussian hierarchy. Layer-specific precision is identifiable only in
the separate directly monitored construction. These are limitations of the
claim, not unresolved contradictions in its current wording.

## Post-correction recheck

Fable re-read the corrected code, raw summary, v11 section, audit, and
Experiment 42 record. It independently recomputed the headroom normalization
and all four paired intervals, verified every cited result, and confirmed the
freeze commit preceded the result commit. It found no fatal or claim-blocking
issue and returned:

- **Minimal precision hyper-loop: yes**
- **Three-operation conjunction: yes**
- **v11 claim ready: yes**
