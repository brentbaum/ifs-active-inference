# Public contract test vector

Contract: `ifs-ai-experiment-51-contract@1.0.1`
Challenge: `51-P-00`
Decision rules: `budget-valid`, `ordering-observed`

## Success

The generic implementation parses the five files, resolves all references,
compiles the three-protector topology and two structure candidates, applies the
broadcast-edge intervention, constructs the declared paired evidence budget,
emits every requested trace field, realizes action success through the declared
outcome table, lets that success enable the declared action-contingent process
for a future exposure transition, and evaluates both decision rules without
bundle-specific code.

## Scientific failure

This public vector has no scientific status. A false decision rule is a
development finding about the dummy apparatus, not evidence for or against a
psychological claim.

## Semantic inexpressibility

At least one locked public construct cannot be parsed, compiled, traced, or
evaluated by the generic path. Because this vector predates private challenge
authoring, the contract or implementation must be corrected before sealing.
