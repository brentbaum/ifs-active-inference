# 51-D4 architecture wall

The generic candidate scorer supports edge-subset structures and imaginal
events now accumulate model evidence. The sealed candidate schema, however,
cannot assign alternative drift or change-point processes to candidates.
Therefore the required five temporal/context generative families cannot compete
within one declared scoring run. Edge-subset recovery is retained only as an
apparatus test and is not renamed D4.
