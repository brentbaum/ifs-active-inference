# V3.6 Round-15 generator repair verification plan

Status: **FROZEN BEFORE REPAIR**  
Authority: accepted `GENERATOR_ONLY` diagnosis and the ten replacement preconditions in `external-round15-rulings.md`.

## Authorized source boundary

Exactly two existing files may change:

1. `ref/v36_round12.py`: in `generate_v3_native_world`, replace the truth-active-count-dependent joint-policy vector with the candidate-common vector `(policy_value, policy_value, policy_value)`.
2. `scripts/run_v36_round12.py`: serialize the already-existing complete native path state in each future Population-A trace row. This is custody output only.

New proof/oracle/test/result files may be added. No likelihood, scorer, posterior aggregation, structure prior, parameter prior, threshold, calibration definition, scientific module, or criterion may change.

## Ten replacement preconditions

1. Exact defect localization remains `GENERATOR_ONLY`.
2. The apparatus-first repair statement is independent of desired ECE direction.
3. The applied source diff is limited to the candidate-common intervention schedule and serialization custody.
4. Frozen scientific model, likelihood, prior, threshold, and calibration-definition hashes remain identical.
5. The repaired staged path identity passes at T=1..4 for all fixtures and all eight factor-ladder steps.
6. The twelve-component complete-data log-joint identity and recombination pass at `1e-10` on zero-seed dummies.
7. The independent 90-digit posterior computation passes at `1e-10` and reports exact zeros.
8. All eight round-13 native-fixture triangulation proofs pass, plus candidate-common schedule semantics.
9. A matched deterministic differential audit proves unchanged structure/parameter draws and conditional emissions; only the intervention schedule and observations causally downstream of it may differ.
10. Seed-map and custody manifests name A-R1 as conditional and unopened; all other batteries and escrows remain closed.

## Execution and stop rule

All checks are deterministic and seed-free. A-R1 (`3722000:3723999`) is not opened. Any failed precondition stops this package without changing criteria or scientific source.

