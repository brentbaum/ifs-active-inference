# V3.6-R1 repair analysis plan

Provenance: **prospective apparatus amendment**, frozen before blocks
`3680000:3683999` and `3684000:3689999` are opened. The original tournament,
margin, and verdict remain in the record and are not reused.

## Bridge qualification

Run all fourteen bridge proofs before any criterion seed. Then consume
`3680000:3683999` once, ascending and gap-free. Seeds `3680000:3681999` form
the own-prior common-document calibration fixture. Seeds `3682000:3683999`
form the fixed-stratum bridge population. Persist every event ledger and
per-world row and hash them before aggregation.

For each model and target publish binary predictive ECE using ten fixed-width
bins, reliability-bin counts/confidence/frequency, Brier score, and mean log
score, overall and stratified by developmental stratum and active-mode count.
Every predictive ECE must be at most `0.05`.

On the own-prior half, predictive-equivalence classes use the exact signature
definition in the bridge spec. Blocking requirements are class ECE `<=0.05`,
95% class-set coverage `>=0.90`, active-count ECE `<=0.05`, and every
load-bearing-edge ECE `<=0.05`. Report 50/80/90/95% class-set coverage,
normalized class entropy, exact-program accuracy/ECE/entropy descriptively,
and a fixed-mixture reweighted-posterior diagnostic that never enters scoring.

V2 precision qualification uses 10,000 deterministic whole-world bootstrap
replicates on the fixed-stratum half. For every family the full 95% interval
width of the mean V2 held-out score must be `<= delta`. A wider interval is an
apparatus stop; the margin is never enlarged.

The 16/32/48-prefix curves are descriptive. The 48-prefix/16-held-out result is
primary.

## Repaired tournament

Only after every proof and qualification passes, consume
`3684000:3689999` exactly once. Use the same canonical R0 document and frozen
adapters. For each target calculate the paired whole-world differences and a
10,000-replicate deterministic bootstrap interval. The criterion is
`lower95(mean D_tau) >= -0.01980262729617973` separately for all five targets.

Publish the five-family Pareto vector, per-family distributions and strata,
support/hash custody, native structure-prior decomposition, and calibration.
The macro mean is descriptive. A valid numerical failure is retained as a
scientific predictive price and does not block later Gates 4–5; an apparatus
failure stops immediately.

No criterion is calibrated on its own data. Diagnosis block
`3690000:3699999`, all escrow, and every prior barred block remain closed.

## Amendment 4 — round-12 native calibration and external tournament

Provenance: **prospective apparatus amendment**, adopted before any seed in
`3690000:3695999` or `3684000:3689999` is opened. This section supersedes the
amendment-3 bridge qualification and generator. The original text above is
retained as the historical declaration; its 4,000-world result remains an
apparatus stop and is now labeled `HYBRID_GENERATOR_DIAGNOSIS_ONLY`.

### Precommitments

Before any criterion seed, freeze the target-semantic audit, permanent proof
15, the external shared-support generator, `shared-target-support-audit.json`,
these calibration definitions, the complete serialization schema, and the
round-12 seed map. Proof 15 independently enumerates the observable posterior
predictive for all five V2 and V3 adapters at `1e-10`. If an adapter disagrees,
only adapter code may change; all frozen V2/V3 scientific sources and
parameters remain byte-identical.

### Frozen calibration definitions

Every world has total weight one. Tokens within a world split that unit weight
equally. Use ten fixed bins `[0,.1)` through `[.9,1]`; report raw bin counts and
effective world weights.

- Binary targets: confidence is `P(target=1)` and outcome is the delivered
  observable token. Report ECE, reliability, Brier, log score, prediction
  entropy, and mean probability assigned to the realized token.
- Active count: report top-label ECE using `max q(K)` against argmax
  correctness, and separately the equal-weight mean of three one-vs-rest
  classwise ECEs. Also report mean `q(K_truth)`, accuracy, multiclass Brier,
  and log score. Truth mass is never paired with argmax correctness.
- Equivalence class: report top-label ECE using maximum class mass against
  whether the top class contains the truth program. Deterministic HPD sets sort
  by mass and then canonical minimum program ID, with coverage at
  50/80/90/95%. Report truth mass, log and Brier-style scores, normalized
  entropy, and exact-program quantities descriptively.
- Each load-bearing edge uses ordinary binary ECE, Brier, and log score.

Every calibration trace serializes before aggregation: all five predictive
vectors; masks and delivered counts; bin assignments; complete factorized
structure posterior; equivalence map and class posterior; active-count and
edge posteriors; truth program/class; confidence and correctness fields. Any
preregistered result not recomputable from the trace is
`FAIL_UNEXECUTABLE — REQUIRED CALIBRATION STATE NOT SERIALIZED`.

### Population B — V2 target-native qualification

On `3690001:3691999`, each seed generates five separately namespaced fixtures.
Seed `3690000` is permanently barred by the evaluator's round-12 precommit
custody adjudication.
Each V2 target module samples from its native prior predictive and forecasts
the same observable token. Per target, all are blocking: ECE `<=0.05`, forecast
normalization error `<=1e-10`, adapter versus direct-enumeration error
`<=1e-10`, and finite Brier/log scores. There is no V2 joint-structure metric.

### Population A — V3 native qualification

On `3692001:3693999`, generate complete documents from the V3 bridge model's
frozen priors, latent processes, and emissions. Blocking: every target ECE
`<=0.05`; equivalence-class top-label ECE `<=0.05`; 95% HPD coverage `>=0.90`;
active-count top-label and macro classwise ECE each `<=0.05`; every
load-bearing edge ECE `<=0.05`; all forecasts normalized and all proper scores
finite.
Seed `3692000` is permanently barred by the same custody adjudication.

### Population C — external qualification

On `3694001:3695999`, generate the external qualification population across
`acute_one`, `chronic_one`, `chronic_multiple`, and `real_danger_adaptive`.
Seed `3694000` is permanently barred by the same custody adjudication; the
one-seed rescope and resulting stratum counts are reported. Both adapters
receive byte-identical documents and the same intervention schedule. V2's
deterministic 10,000-replicate bootstrap interval width must be at most
`delta=log(1.02)` for every target. External calibration is descriptive and is
reported overall and by stratum.

### One tournament

Only if all blocking qualification items pass, consume `3684000:3689999`
once: 1,500 worlds per stratum. For every target, report paired
`D=S_V3-S_V2` and its deterministic 10,000-replicate whole-world bootstrap
interval. The familywise criterion remains
`lower95(E[D]) >= -0.01980262729617973`. Strata have no new thresholds. A
valid numerical failure is a retained predictive cost, not an apparatus stop.

Gate 4 may run after the precommitment commit. Gate 5 remains closed in this
dispatch.
