# V3.6 COMPOSE analysis plan

Status: prospectively frozen before the re-scoped attainability pilot. The
Stage-0 custody adjudication permanently bars seed `3600000`; the pilot is
`3600001:3603999`. C-V36A/B/C and their escrow remain evaluator-only.

## Pilot-fidelity amendment: premature versus post-revision do-over

Provenance: `results/V3.6/stage0-adjudication.md`. The first pilot stop is
retained. It declared the premature-do-over contrast as an equivalence, which
contradicted the already sealed V3.3 claim structure. This amendment is frozen
before the fresh pilot and uses no first-pilot numeric value.

Post-revision do-over remains the V3.3 equivalence: once the ordinary
configural evidence has revised the structure, the imaginal episode has no
privileged operation. The prospectively repaired declaration was:
~~premature do-over is a positive causal effect, with exact estimand
`q_current_edge_absence(full) -
q_current_edge_absence(premature_do_over)`~~.

Both schedules are indexed to the root-revision event observed in each world.
Premature imagery must end before that world's event; post-revision imagery
begins only after it. No fixed slice is a revision boundary. A runtime audit
checks this identity on every composed world, and a regression fixture moves
the event boundary to prove the schedule follows the event.

The original pilot `3600001:3603999` and seed `3600000` are barred. The fresh
pilot `3660000:3663999` reruns all ten contrasts once. The remainder
`3664000:3679999` remains diagnosis-only.

### Adjudication 2 — retained endpoint path-independence finding

`results/V3.6/stage0-adjudication-2.md` governs. The fresh pilot is retained
and barred. Premature do-over is now **descriptive only**, in the V3.1
revisability reporting class. It carries no floor and no gate criterion. No
third construct may be piloted.

Every downstream profile reports both observations verbatim:

- first pilot: mean `0.015139500753264512`, 95% interval
  `[-0.018567536075274952, 0.04911686181384986]`;
- fresh event-indexed pilot: mean `-0.007591287369016907`, 95% interval
  `[-0.03688060730166053, 0.021721859932301718]`.

The retained interpretation is endpoint path-independence under sufficient
later corrective evidence. In sealed V3.3, premature imagery alone produced
exactly `0.0` durable reduction; in the composed trajectory, later ordinary
corrective evidence redoes the work. This leaves the post-revision equivalence
below unchanged.

## Order and custody

Gate 1 runs without development worlds. After Gate 1 passes, the 3,999 pilot
seeds are consumed once, ascending and gap-free. Each worker persists the
runtime event ledger and world record to JSONL. Record and whole-file SHA-256
hashes are written before aggregation, printing, or process exit. A second
custody failure is an immediate honest stop.

The pilot is barred after consumption. It may freeze thresholds but may never
evaluate them. Gates 2--5 remain closed until the evaluator seals C-V36A/B/C.

## Gate 1: permanent composition battery

The battery retains the V3.0--V3.5 semantic batteries and expressly checks:

1. no V2 scientific-stage import in the V3 composed model;
2. no new latent, likelihood, prior, update equation, or scientific branch;
3. immutable pure readouts and the one-posterior audit;
4. independent readout recombination with copied inputs;
5. independent recombination of all code-length terms;
6. `L_total = L_grammar + L(H) + L(theta|H) + L(protocol)`;
7. dormant emission counted once and active count charged only in `L(H)`;
8. expanded marginal-calibration/common-support item 17 remains passing;
9. candidate-common registration delivered/masked identity;
10. stakes-in-utility scientific-posterior identity;
11. regulation-only root-BF zero identity;
12. interventional independent/opposed/allied fingerprint fixture;
13. restricted-prior lesion and masked-channel identities;
14. no analysis/protocol label reaches inference;
15. all inherited freeze manifests verify through their committed chains;
16. the compression registry includes every V3.5 repair factor and constant.

## Pilot estimands fixed before observation

All nonzero effects are same-seed paired contrasts. Intervals are deterministic
whole-world 95% bootstrap intervals. Each row declares its direction and kind.

| Estimand | Direction | Kind |
|---|---|---|
| full minus regulation-without-root `root_evidence_uptake` | positive | causal effect |
| full minus cue-only `root_transfer` | positive | causal effect |
| full minus mode-bypass `q_policy_open` | positive | causal effect |
| full minus noncontingent-soothing `q_partner_reliable` | positive | causal effect |
| full minus unreliable-partner `q_partner_reliable` | positive | causal effect |
| full minus broadcast-off `root_evidence_uptake` | positive | causal effect |
| full minus denied-contact-masked vulnerable `contact_response` | positive | causal effect |
| full minus context-disabled `q_context_specific` | positive | causal effect |
| full minus pruning-disabled `q_current_edge_absence` | positive | causal effect |
| full minus premature-do-over `q_current_edge_absence` | no criterion; both pilot estimates reported | descriptive retained finding |
| low/high stakes maximum scientific-posterior distance | `<=1e-10` | exact identity |
| low minus high stakes `q_policy_open` | positive | causal policy effect |
| active mode-count truth/recovery and edge recovery | positive accuracy | recovery |
| higher-slot nonmissing support under smaller candidates | finite/common support | exact identity |
| mixed masking/dormancy normalization | sums to one | exact identity |
| targeted-all minus targeted-one mode-3 support | positive | causal effect |
| engagement/exclusion joint-policy-edge uptake | declared positive evidence-path effects, separately | causal effects |
| independent/opposed/allied signed `D` | zero/negative/positive | identity and signed effects |
| V3.1 revisability | effect plus interval only | descriptive retained finding |
| V3.3 post-revision do-over | full interval inside ROPE | equivalence retained finding |
| V3.4 whole-program accuracy at 32/48/96 | reported by length | information curve |

The common-world compression profile additionally reports, separately for V2
and V3 where their public APIs expose the quantity: posterior calibration,
credible-set coverage, active-count recovery, edge recovery, common-support
equality, held-out log predictive density, regret to generator, and structural
description length. No scalar aggregate is permitted.

## Mechanical pilot-to-floor rule

The rule is frozen before the pilot and applies without contrast-specific
selection:

1. A nonzero effect is attainable only if its paired 95% interval carries the
   declared sign.
2. Every nonzero effect floor is exactly `0.50 * abs(pilot point estimate)`.
3. Every rate/recovery floor is exactly `0.90 * pilot point estimate`.
4. Calibration/proper-score ceilings equal the pilot point estimate plus
   `0.03`.
5. Exact identities use `1e-10`; equivalence uses the full 95% interval inside
   the theory-defined ROPE `[-0.01, 0.01]`.
6. No primary-length floor is transplanted to an information-budget sweep.

The held-out noninferiority margin is not fitted to V3. It is frozen at
`0.018566762350958` nats/token: the full width of the deterministic 95%
bootstrap interval for the mean V2.4.4 96-slice BMA score over 2,500 committed
public worlds (`[-1.89879339, -1.88022663]`, RNG seed 3606, 10,000
replicates). The criterion is V3 minus V2 lower CI `>= -margin` on common
worlds.

## Later gates (closed pending evaluator seals)

Gate 2 uses `3604000:3613999` for composed recovery, calibration, support,
common-world predictive scoring, and structural economy. Gate 3 uses
`3614000:3629999` for whole therapy, all ten comparators, round-10 stress
cells, and the V2/V3 Pareto profile. Gate 4 uses `3630000:3634999` for the
complete selective lesion matrix with restricted-prior identities. Gate 5
uses `3635000:3659999` for cumulative regression and robustness. Diagnosis
`3660000:3679999` and escrows `4100000:4129999` remain closed.

Scientific failures are published rather than tuned. The V3.1 revisability
limitation, V3.3 do-over equivalence, V3.4 short-history information curve,
and both V3.5 failure-record items remain visible throughout.
