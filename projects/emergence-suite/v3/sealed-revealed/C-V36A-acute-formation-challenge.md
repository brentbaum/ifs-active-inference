# Sealed challenge C-V36A — acute single-mode formation through full therapy

**Sealed by the evaluator after the V3.6 stage-0 mechanical freeze (STAGE0_FREEZE_READY_AWAITING_C_V36A_B_C_SEALS), before any gate 2–5 seed, per the amendment-1 custody pattern. Design rule honored throughout: frozen numeric floors apply ONLY to cells replicating the pilot's exact default configuration (mode_count=3, topology='allied', stakes='low', support_target='all', policy_regime='engagement', missingness=0.0, length=16, varying protocol only); single-mode cells carry prospective SIGN and IDENTITY criteria only — no floor is transplanted across configurations (the V3.4 lesson). Pre-seal linter: exact-artifact dry-run; ComposeConfig vocabulary validation; released_block threading; escrow containment; traced attainability pilots on diagnosis-reserved seeds with persisted event ledgers (barred; recorded in the ledger entry). Estimators are CompositionReadout field differences on same-seed pairs exactly as the pilot computed them.**

## Cells (parse instruction binding)

{'parse_instruction': "Paired cells consume ONE escrow seed per pair: full_readout = run_therapy(seed, full_config), ablation_readout = run_therapy(seed, ablation_config), both with released_block=(4100000,4102999), inside serializing trace contexts whose event ledgers are persisted to results JSONL with hash records BEFORE aggregation or output; paired difference = full field minus ablation field. Single-config cells run one world per seed. Base config: ComposeConfig(protocol=<named>, mode_count=<named>, topology='allied', stakes='low', support_target='all', policy_regime='engagement', missingness=0.0, length=16). ast.literal_eval on the exact bracketed text only.", 'cell_a1_regulation': {'escrow': '4100000:4100799', 'n_pairs': 800, 'mode_count': 1, 'full': 'full', 'ablation': 'regulation_without_root_evidence', 'field': 'root_evidence_uptake'}, 'cell_a2_cue_only': {'escrow': '4100800:4101499', 'n_pairs': 700, 'mode_count': 1, 'full': 'full', 'ablation': 'cue_only_exposure', 'field': 'root_transfer'}, 'cell_a3_pruning': {'escrow': '4101500:4102199', 'n_pairs': 700, 'mode_count': 1, 'full': 'full', 'ablation': 'structural_pruning_disabled', 'field': 'q_current_edge_absence'}, 'cell_a4_single_mode_full': {'escrow': '4102200:4102999', 'n_worlds': 800, 'mode_count': 1, 'protocol': 'full'}}

## Criteria

1. *(scientific)* Cell a1: the paired full-minus-ablation `root_evidence_uptake` 95% whole-world bootstrap interval carries the positive sign — an acutely formed single-mode organism takes up root evidence only through regulated therapy.
2. *(scientific)* Cell a2: paired `root_transfer` interval positive — cue-only exposure does not transfer to the root in the single-mode organism.
3. *(scientific)* Cell a3: paired `q_current_edge_absence` interval positive — without structural pruning the acute burden edge remains.
4. *(semantic + reporting)* Cell a4: `q_external_danger`, `q_identity_organization`, and `L_total` are finite in EVERY world, and the population distribution of all four cross-mode fingerprints is published DESCRIPTIVELY with no floor. Expressibility note, binding: the exact single-mode zero-influence identity was sealed at V3.5 under truth-restricted scoring (`restrictions={'active_modes': ...}`); the composed public readout reports posterior-model-averaged influence, which carries residual mass on multi-mode candidates and provably cannot express that identity — the pre-seal pilot measured |D| <= 0.013 at n=30, reported here so the descriptive claim is calibrated in advance. Re-declaring the identity against the averaged readout would repeat the V3.6 stage-0 class of construct mismatch; it is deliberately not done.
5. *(custody)* All 3,000 escrow seeds `4100000:4102999` consumed exactly once, ascending, gap-free; event ledgers persisted with hash records at execution; the escrow remainder `4103000:4109999` is retired unconsumed by this seal; verdict classes recorded before interpretation.

Pass = all five. Failure interpretations pre-committed: criterion-4 violation is the most serious class (phantom cross-mode influence in a single-mode organism falsifies the dormancy claim under composition); criteria 1–3 failures localize the named therapy pathway at single-mode scale; apparatus otherwise. Software errors: invalidate-and-repeat with byte identity.
