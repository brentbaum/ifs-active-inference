# Sealed challenge C-V32 — SPLIT challenge: witnessed redescription and temporal structure

**Sealed by the evaluator after the V3.2 freeze (FREEZE_CANDIDATE with CUSTODY_NOTE_PREFLIGHT_UNSERIALIZED, manifest 42/42 evaluator-verified; the custody note is carried, not cleared). Pre-seal linter (full accumulated set, all evaluator runs under serializing trace contexts): exact-artifact dry-run of every cell; scope/dynamics vocabulary validated against v32.SCOPES/DYNAMICS; released_block threading verified; escrow containment; attainability pilots on the EXACT sealed configurations (barred blocks 3232000:3233199, 60 worlds/cell, traced): c1 P(true structure) .9766 / P(outcome context scope) 1.0000; c2 P(true) .6316 / P(context scope) .2000 (scope neutrality holding — a single regime leaves scope near prior); c3 (the mixed world inexpressible under v2's menus) P(true) .9599; c4 P(true) .5316 / false-recurrence .2577 (against the frozen .29 ceiling). Floors sit inside piloted values with margins.**

## Cells (parse instruction binding)

{'parse_instruction': 'Cells declare TemporalStructure kwargs, evidence_style, and length; worlds via generate_world(seed, structure=TemporalStructure(**structure), length=length, evidence_style=evidence_style, released_block=(4020000,4023999)) inside a serializing trace context; scoring via public score_world with the method readouts named per criterion. ast.literal_eval on the exact bracketed text only.', 'cell_1_witnessed_split': {'escrow': '4020000:4020999', 'n_worlds': 1000, 'structure': {'active_contexts': 2, 'scopes': ('context_specific', 'context_specific'), 'dynamics': ('static', 'static')}, 'evidence_style': 'witnessing', 'length': 48}, 'cell_2_single_regime': {'escrow': '4021000:4021999', 'n_worlds': 1000, 'structure': {'active_contexts': 1, 'scopes': ('shared_global', 'shared_global'), 'dynamics': ('static', 'static')}, 'evidence_style': 'single_regime', 'length': 48}, 'cell_3_mixed_drift_recurrent': {'escrow': '4022000:4022999', 'n_worlds': 1000, 'structure': {'active_contexts': 2, 'scopes': ('cue_specific', 'context_specific'), 'dynamics': ('ordered_random_walk', 'discrete_recurrent_context')}, 'evidence_style': 'natural', 'length': 64}, 'cell_4_one_way_no_recurrence': {'escrow': '4023000:4023999', 'n_worlds': 1000, 'structure': {'active_contexts': 2, 'scopes': ('context_specific', 'context_specific'), 'dynamics': ('one_way_change', 'one_way_change')}, 'evidence_style': 'natural', 'length': 48}}

## Criteria (population statistics over the frozen score_world method readouts)
1. *(scientific)* Cell 1 (witnessed split): mean structure_probability(generating structure) >= .90; mean scope_probability('outcome_emission','context_specific') >= .95; witnessing gain vs the frozen single-regime comparator (the stage's gate-3 construct) reported with its interval; historical-parameter query error <= 1e-10 (retention exact).
2. *(scientific)* Cell 2 (single regime): mean scope_probability('outcome_emission','context_specific') <= .30 — scope neutrality holds prospectively (no split identified from one regime); mean structure_probability(generating) >= .50.
3. *(scientific)* Cell 3 (mixed drift + recurrent — the C-V24 world): mean structure_probability(generating) >= .85; dynamics recovered per block (cue block ordered_random_walk and outcome block discrete_recurrent_context each argmax in >= .85 of worlds).
4. *(scientific)* Cell 4 (one-way, no recurrence): mean dynamics_probability('outcome_emission','discrete_recurrent_context') <= .29 (the frozen false-recurrence ceiling) — a change that never returns is not recurrence.
5. *(semantic + custody)* Posterior normalization audit (1e-10); escrow by ledger; 4000 seeds once ascending gap-free; per-world traces serialized and hashed at execution before criteria; verdict classes; the stage custody note restated in the verdict.

Pass = all five. Failure interpretations pre-committed: cell-1 failures localize witnessed redescription (the stage's central claim); cell-2 failure violates scope neutrality prospectively (construct regression); cell-3 failure breaks the compression thesis's expressibility promise; cell-4 failure conflates change with recurrence; 5 = apparatus. Software errors: invalidate-and-repeat with byte identity.
