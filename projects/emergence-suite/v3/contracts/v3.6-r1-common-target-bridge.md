# V3.6-R1 common-target bridge contract

Status: **frozen before criterion seeds**. Provenance: Gate-3 adjudication
amendment 3, external round-11 rulings 1–5 and 10.

The original V3.6 Gate-3 tournament remains `FAIL_INVALID_COMMON_SUPPORT_APPARATUS`.
It is never overwritten or rerun. This bridge defines the one authorized
apparatus repair.

## Canonical document

Each seed produces one typed R0 world and one immutable observation document:
64 slices, a 48-slice inference prefix, and a 16-slice untouched held-out
suffix. The document is generated before either model adapter runs. Only the
bridge generator owns observation RNG. V2 and V3 receive deep copies whose
world, observation, and held-out-target SHA-256 values must agree per seed.

The five delivered target families are identity, outcome under the same
`do(action)`, context/localization, partner response, and contact response.
`None` means masked and contributes no token. No sentinel is an observation.
Action is an intervention input and is never an action-selection likelihood.

Each V2 family has exactly one scorer: V2.3.2 formation, V2.3.4 attribution,
V2.4 redescription, V2.6a partner inference, and V2.6b protector forecasting,
respectively. Their likelihoods are never multiplied into a pseudo-joint.
V3 uses its native structural prior and exact model average. Model-exclusive
channels are scope extensions only and cannot enter the criterion.

## Scoring and equivalence

For model `m`, world `w`, and family `tau`, the score is the mean log
probability of the delivered held-out target tokens under the posterior fixed
after the 48-slice prefix. `D_tau = S_V3 - S_V2`. The result is a five-element
Pareto vector. An equal-family macro mean is descriptive only.

Programs are predictively equivalent when the maximum absolute difference
between their exact predictive signatures is at most `1e-10` over every value
of all five families on the frozen query schedule. Calibration sums posterior
mass over these classes. Exact-program statistics remain descriptive.

The prospective familywise noninferiority margin is
`delta = log(1.02) = 0.01980262729617973` nats per delivered target token.
Every family independently requires the paired whole-world bootstrap lower
95% endpoint to be at least `-delta`. No aggregate can rescue a family.

## Permanent pre-criterion proofs

1. Canonical document identity.
2. Held-out target-token identity.
3. Mask identity.
4. Equal delivered-target counts.
5. No sentinel counted as an observation.
6. Deterministic adapters with zero RNG.
7. Normalized predictions on every shared target.
8. Held-out target unavailable before prediction.
9. Full posterior predictive includes each native structural prior.
10. Truth/equivalence-class-clamped decomposition recombines exactly.
11. No model-exclusive channel enters a primary score.
12. No V2 module contributes twice to one target.
13. Bridge inputs are copied.
14. Frozen model source and scientific parameters remain bitwise unchanged.
15. Forecast-semantics identity: for identity, outcome, context, partner, and
    contact, each V2 and V3 adapter's probability vector equals a separately
    authored exact enumeration of the frozen module's posterior predictive for
    the observable target token within `1e-10`. Latent posterior mass is never
    substituted for an observation forecast.

Any proof failure is an apparatus stop before criterion seeds.

## Round-12 population separation

Amendment 4 supersedes the amendment-3 population design without deleting its
record. The consumed 4,000-world hybrid is permanently
`HYBRID_GENERATOR_DIAGNOSIS_ONLY`; it is not native calibration and is not the
tournament population.

Population B comprises five separately namespaced V2 native-prior fixtures per
seed. Each frozen module generates and forecasts its own observable target;
there is no invented V2 joint model. Population A is the complete prior
predictive of the V3 bridge model. Population C is an external finite-state
generator owned by neither model. It uses a persistent Markov partner process,
the public V3.2 context path and emission functions, a shared `do(action)`
schedule, and only target values with finite nonzero support under both
adapters.

The calibration trace is lossless for every preregistered calculation. It
contains all predictive vectors, masks, counts and bins, the factorized but
complete structure posterior, the factorized equivalence-class map and class
posterior, active-count and edge posteriors, truth identifiers, and every
confidence/correctness field. An absent required field is
`FAIL_UNEXECUTABLE`; a consumed block is never rescored.
