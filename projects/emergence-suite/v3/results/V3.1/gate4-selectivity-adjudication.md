# V3.1 gate-4 selectivity adjudication (evaluator, 2026-07-30; V2.5b gate-4 pattern)

The repaired gate-4 verdict remains **FAIL as recorded**. The failed statistic — absolute W->Y posterior movement <= 0.20 after the identity-edge lesion — is reclassified as an **unaudited criterion operationalization** (audit items 2 and 4): under exact finite model comparison, deleting identity-bearing programs renormalizes the posterior over survivors, so absolute movement of a retained production is mechanically expected and conflates renormalization with damage. The construct "each production deletion removes only its declared consequence" has an exact operationalization for Bayesian lesions: **deletion is conditioning of the structure prior and nothing else**, so the lesioned posterior must equal the posterior recomputed from the restricted prior — an identity at 1e-10, which also detects any genuine likelihood perturbation of survivors.

Binding for gate-4 re-evaluation and all future V3 lesions: (1) declared-consequence check as written (part-like posterior mass exactly 0 after the identity-edge lesion — already held 333/333); (2) restricted-prior consistency identity <= 1e-10 per world (blocking); (3) absolute posterior movement of retained productions reported descriptively with the renormalization decomposition (never a floor). No world re-generation — the retained 2,000 gate-4 worlds are re-scored under the corrected statistics (re-scoring retained traces with a corrected readout is the V2.4.2/V2.5a precedent; the original FAIL stays in the ledger).

On identity-and-consequence pass: proceed to gate 5 under the standing continuation. Disclosed at the next consultation.

## Amendment (evaluator, 2026-07-30): deterministic reconstruction permitted
The "no regeneration" clause was intended to bar NEW worlds or seeds, not deterministic reconstruction of the already-consumed block. Per the V2.4.x diagnosis precedent ("deterministically reconstructs only their already-consumed worlds through the frozen constructors; no new seed consumed"), the corrected statistics are computed by reconstructing the consumed 3110000:3111999 worlds through the frozen seed-keyed generators. Custody requirements: (1) before applying corrected statistics, the reconstruction must exactly reproduce the originally recorded repaired-gate-4 aggregates (per-cell finiteness counts, part-mass-zero 333/333, the 0.5465 movement rate, normalization maxima) — any mismatch stops as a reproduction failure; (2) all future V3 gate runners serialize per-world traces and hashes at execution time (binding henceforth). No new seed is consumed.
