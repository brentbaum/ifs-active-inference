# V3.4 stage-0 repair authorization (evaluator, 2026-07-30)

Classification: pure software error in the recovery generator (the V2.6a generator/scorer-mismatch class, caught by the traced pilot before any criterion seed). The generator passes the MATCH probability c = p(O_G = G) to bernoulli(), whose argument must be p(O_G = 1); for G=0 the emitted distribution is therefore wrong, driving root observations toward 1 regardless of truth (pilot root accuracy 0.475, root ECE 0.304, with structure ECE 0.032 and edge accuracy 0.878 unaffected — the localization is exact).

Authorized, narrowly: the generator emits O_G=1 with probability c when G=1 and 1−c when G=0 (matching the declared scorer likelihood identically); a gate-1-class identity test pins generator/scorer distributional agreement on enumerable dummies (both G values); regression test. Fresh evaluator-allocated pilot block 3430000:3431999 (recorded here) for the corrected traced pilot; floors frozen from it before criterion blocks; the consumed 3400000:3401999 pilot retained as the defect record; gate blocks 3402000:3419999 remain valid; escrow untouched. Calibration-by-theorem applies to the corrected pilot: with the generator sampling the scorer's own model, root ECE must fall to the ~0.03 range structure already shows — a large residual would indicate a second defect and stops again.
