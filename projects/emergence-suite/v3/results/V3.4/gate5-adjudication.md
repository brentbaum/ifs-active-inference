# V3.4 gate-5 adjudication (evaluator, 2026-07-30; V2.4.4-BMA/V2.5b-transplant pattern)

The frozen gate-5 verdict remains **FAIL as recorded**. The failed check — 32-slice whole-program (four-edge conjunction) accuracy 0.733 against the 0.78 floor — is reclassified as an **unaudited information-budget transplant** (audit items 4 and 5): the floor was frozen from the gate-2 pilot at the primary 48-slice budget; the exact posterior is Bayes-optimal, so its conjunction accuracy at 32 slices is the information bound of the shorter history, mechanically below the primary-length floor. Every per-edge accuracy passes at 32 slices (min 0.845), calibration is theorem-clean (structure ECE 0.020, root ECE 0.013), and the 96-slice cell passes. The standing suite convention (every v2/v3 gate-5): primary-cell thresholds bind only the frozen primary cell; robustness cells report sign, interval, and localization.

Binding: the 32-slice cell is scored under the robustness convention — descriptive, with the 32/48/96 information curve and per-edge decomposition published; the primary 48-slice cell remains blocking at its frozen floors (and passes). No number changes; the transplanted application's FAIL stays in the ledger. Freeze proceeds with the stage disposition noting the short-history conjunction bound. Disclosed at the consolidated consultation.
