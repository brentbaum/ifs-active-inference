# V3.4 RELATE stage report

Status: **FROZEN_ADJUDICATED_SHORT_HISTORY_CONJUNCTION_BOUND**.

V3.4 activates one latent partner process and four generic grammar
productions: partner-to-local-precision, partner-to-outcome,
partner-action-to-relational-observations/outcome, and partner-state
transitions. Trust and co-regulation remain pure posterior readouts.

Gate 1 passed all semantic proofs. Regulation without typed root evidence
produced root BF exactly zero; broadcast-off preserved local relational
inference; the independently authored oracle agreed within tolerance.

The initial Stage-0 pilot exposed a recovery-generator software defect. Its
root observation sampled `p(O_G=G)` where the Bernoulli interface required
`p(O_G=1)`, producing root accuracy `0.475` and ECE `0.304`. The authorized
repair restored generator/scorer identity for both root values. The corrected
pilot produced root accuracy `0.791` and ECE `0.019`, comparable to structure
ECE `0.027`.

Gate 2 passed exact recovery, calibration, coverage, exact-log-probability,
and independent-oracle criteria. Gate 3 passed the complete factorial and
control battery. Regulation without root evidence remained exactly neutral;
regulation increased local precision by `0.4248`, root uptake by `0.0740`,
and transfer by `0.0518`. Broadcast-off preserved local inference exactly.

Gate 4 passed all five lesions. Restricted-prior consistency held within
`2.75e-14`, the independent oracle within `5.39e-14`, and root-evidence
masking was exactly root-neutral.

The original Gate-5 record remains formally **FAIL** because the 32-slice
exact-program result `0.733` was compared with the primary 48-slice floor
`0.78`. `gate5-adjudication.md` classifies this as an unaudited
information-budget transplant. Under the standing convention, the primary
48-slice cell is blocking and passed at `0.837`; 32- and 96-slice cells are
descriptive. Exact-program recovery followed `0.733 / 0.837 / 0.915` across
`32 / 48 / 96` slices. At 32 slices every individual edge passed, with minimum
accuracy `0.845`, while structure ECE was `0.020` and coverage `0.975`.

All scientific robustness cells, trace ledgers, and V3.0–V3.3 manifest checks
passed. Final regression: V3 `43/43` tests and cumulative V2 `26/26` modules.
Both V3.4 information bounds remain frozen:
`B_max_v34_relational = 3.8066624897703196` and
`B_max_v34_root = 1.9736255489018601`.

C-V34 escrow `4040000:4043999` remains untouched.
