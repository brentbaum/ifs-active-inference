# Decisive-tests Round-26 adjudication (INTERNAL)

**Authority.** Evaluator (Fable) as advisor per Brent's standing 2026-08-03
instruction; INTERNAL labeling, open to retroactive external review. The
classification below stays within the round-24 ratified precedent: all
three defects are runner/aggregation-side, directly localized to cited
lines, with the scientific computation untouched — the class for which
direct localization lawfully replaces the five-layer ladder.

## Ruling 26.1 — S2-A and S2-B stand as scientific results

Their cells never invoke the S2-C arm override, their statistics are
reproducible from persisted traces, and their prediction rows are scored
from the retained immutable records: S2-A gated-descent modal ordering
0.918 with all three control cells selecting their intended non-IFS
strategies (the authored-optimality falsifier did not trigger); S2-B
eventual-contact 0.821 with all three capacities, 0.0 for partner-only,
contact-only, and neither, 0.422/0.492 for single removals — the
registered joint-insufficiency/integration claim shape is met.

## Ruling 26.2 — S2-C is a retained apparatus stop

FAIL_APPARATUS_ESTIMAND for the bypass/backlash battery: (1) the
forced-contact latch authorizes every later controller contact instead of
the one probe; (2) the aggregate scores eventual contact including the
probe, so the forced arm equals 1.0 by construction and the registered
LATER-contact direction is unscoreable; (3) the low-permission arm
overrides the controller rather than requesting contact under low
permission as registered. The S2-C prediction rows are marked
NOT_EVALUABLE — neither met nor unmet — and the original immutable records
are retained unedited (hashes in the stop record).

## Ruling 26.3 — repair and replacement block

Authorized, runner-only: reset the forced latch after the single probe
slice; score later contact strictly after the probe event; implement the
bypass arm as registered (controller policy conditioned on low permission,
no override). Differential audit; frozen scientific modules and the S2
design-freeze scientific content unchanged. One replacement S2-C block:
`3820000:3823999`, one-shot, full custody, per-arm serial first worlds.

## Ruling 26.4 — permanent estimand-conformance proof

New standing rule joining the defense battery: before any block with
registered arm-level outcome statistics opens, a zero-seed dry run must
prove every registered statistic is computable and non-degenerate under
every arm — an arm whose registered outcome is constant by construction
fails the block before it opens. This converts the S2-C defect class from
post-verdict discovery to pre-block.
