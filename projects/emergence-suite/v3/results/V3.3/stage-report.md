# V3.3 PRUNE stage report

Status:
**FROZEN_ADJUDICATED_MIXED_DO_OVER_NULL_AND_SUGGESTION_DIRECTION**.

V3.3 demonstrates reverse posterior movement over the same `M1_G`, `G_W`,
`G_A`, and `G_Y` existence spikes used by V3.1 GROW. It introduces no
reduction operation. Mode existence and the historical graph remain queryable;
candidate-common neutral observations contribute no structure evidence.

Gate 1 passed every semantic proof. The original Stage-0 pilot stopped because
the fixed do-over schedule occurred after material reduction. The
evaluator-authorized event-indexed repair then passed its one-shot attainability
condition and froze a small positive speedup floor before criterion worlds.
Gate 2 passed exact recovery, calibration, coverage, exact-log-probability, and
independent-oracle checks.

Gate 3 remains formally **FAIL**. Configural correction produced material
reduction in `0.9775` of worlds, suggestion-only false reduction was `0.0`,
premature durable reduction was `0.00429`, correction-only reduction was
`0.98286`, adaptive-edge survival was `0.99802`, and neutral error was exactly
zero. The two failed families were:

- timely do-over speedup `-0.0003125`, 95% interval
  `[-0.0009375, 0.0]`, versus floor `0.00025`;
- suggestion root direction `-0.007463967250969002`, against the frozen
  positive direction.

`gate3-adjudication.md` retains both findings and authorizes them as the only
non-blocking families. Gate 4 subsequently passed all blocking lesion checks:
restricted-prior identity error was at most `6.56e-15`, independent-oracle
error was zero, all deleted-edge masses were exactly zero, and imaginal-channel
masking was exactly neutral with paired no-do timing identity `1.0`.

Gate 5 passed every blocking recovery, scientific-robustness, trace-custody,
and cumulative-regression check. The limitations repeated verbatim: do-over
speedup was `0.0 [0.0, 0.0]`; suggestion direction was
`-0.004940118398975422`
`[-0.0117681634573112, 0.001722225536766276]`. All V3.0–V3.2 freeze manifests
rehash cleanly.

All trace bundles are below 90 MB. Their per-record and whole-file SHA-256
ledgers are included. Escrow `4030000:4033999` remains untouched.

Final regression: V3 `38/38` tests and the cumulative V2 `26/26` test modules
passed.
