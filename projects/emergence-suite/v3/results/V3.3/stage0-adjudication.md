# V3.3 stage-0 adjudication: event-indexed do-over timing (evaluator, 2026-07-30)

Classification: estimand-timing defect caught prospectively by the attainability pilot (zero criterion seeds; pilot consumed and barred; gate-1 identities incl. episode-label neutrality all passed — the typed-evidence interface is behaving exactly as designed, and the zero is a scheduling artifact, not evidence about do-overs).

Ruling: the draft hard-coded the imaginal episode's availability at slice 18; the spec's construct is a POST-ROOT-REVISION do-over. The correction is construct fidelity, not tuning: episode availability becomes EVENT-INDEXED — the first slice after the world's root-revision event (with the premature arm indexed before it, preserving the premature/timely contrast) — so the acceleration estimand measures what the construct names. No likelihood, prior, evidence semantics, or readout changes; only the protocol's episode scheduling, which the R0/protocol layer declares publicly.

Authorized cycle: (1) event-indexed scheduling in the public protocol declaration; (2) fresh traced pilot on 3330000:3331999 (evaluator-allocated; recorded here) with same-seed pairs; floors frozen from it before criterion blocks; ONE-SHOT — if the event-indexed design still yields zero attainable acceleration, stop and report that as the finding (do-overs may genuinely not accelerate an already-fast reduction; that would be a publishable result, not a defect); (3) the consumed 3300000:3301999 pilot retained as the design record; gate blocks remain valid; escrow untouched. Disclosed at the next consultation.
