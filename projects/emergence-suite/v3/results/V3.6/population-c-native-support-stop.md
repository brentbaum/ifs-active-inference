# V3.6 Population C native-support apparatus stop

Status:
**HONEST_STOP_APPARATUS_TRUTH_PROGRAM_OUTSIDE_NATIVE_CALIBRATION_SUPPORT**.

The evaluator-authorized replacement Population C block
`3724000:3725999` passed its cardinality preflight. The runner serialized an
ascending, gap-free prefix of 1,500 worlds (`3724000:3725499`) before the
next ordered world failed in the `real_danger_adaptive` stratum.

The failure was:

> AssertionError: truth program missing from native support

Apparatus localization is direct from the frozen code. The external
`real_danger_adaptive` constructor declares a one-active-mode structure with
`cross_sign=-1`. The native calibration-state routine requires the exact
truth structure/sign/partner tuple to occur in its native posterior support;
it cannot find this tuple and raises after world generation and model
scoring, before the row can be serialized.

No criterion aggregation or evaluation began. The persisted prefix contains
500 worlds from each of `acute_one`, `chronic_one`, and `chronic_multiple`.
Its trace is 324,764,712 bytes with SHA-256
`4ea857b94caaf1d51731fed9ca8c3e2a19072dc857a0b51def09bf2a8a57de41`.

Custody cannot be certified for the unpersisted suffix. Seed `3725500` was
necessarily generated and scored before the calibration assertion, but was
not serialized. Because ordinary parallel dispatch was active, the retained
trace cannot establish exactly which later in-flight seeds were also
executed. This is therefore an apparatus and custody stop, not a scientific
Population C result.

No retry or repair is authorized. The tournament, Gate 4, Gate 5,
compatibility attestations, freeze manifest, and all escrows remain unopened
pending evaluator adjudication.
