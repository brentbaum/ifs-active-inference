# V3.6-R1 Round-14 Population-A diagnosis stub

Verdict: **STOP — blocking Population-A qualification failure**.

All 2,000 replacement worlds (`3714000:3715999`) serialized once, ascending
and gap-free. The trace hash verifies as
`88eaa8ddcd888a0d6fee02741df4109f9bfac422d0dcb5e708bba1e5dcefa8e3`.

Three frozen criteria failed and are retained verbatim:

- `active-count top-label ECE > 0.05` — observed `0.0845007241`;
- `active-count macro classwise ECE > 0.05` — observed `0.0973279833`;
- `edge JOINT_POLICY_Y ECE > 0.05` — observed `0.0513375512`.

All five target-predictive ECEs passed (maximum `0.0308713519`), the
equivalence-class top-label ECE passed (`0.0293685134`), and 95% class-set
coverage passed (`0.964`). No diagnosis, repair, criterion change, or rerun is
self-authorized. Population C, the tournament, Gate 4, Gate 5, compatibility
attestations, freeze, barred blocks, and escrows remain unopened.
