# V3.6 gate-3 adjudication — amendment 3 (external round-11 rulings adopted)

Evaluator: Fable. Date: 2026-07-31. Source: external advisor round-11
numbered rulings, verbatim in `external-round11-rulings.md` (clipboard
capture; scattered replacement characters for em dashes/minus signs — content
otherwise exact). Adopted as binding in full. Where they conflict with any
earlier V3.6 record, these rulings govern.

## Core adoptions

1. **Classification**: the gate-3 noninferiority component is
   `FAIL_INVALID_COMMON_SUPPORT_APPARATUS`. The FAIL verdict and the original
   −0.0339 [−0.0380, −0.0296] result are retained verbatim; it is not
   evidence of predictive inferiority. The structural prior/model-averaging
   cost stays in the repaired test (no truth-clamping in the primary
   statistic; the clamped decomposition remains diagnostic).
2. **Repaired design — V3.6-R1 COMMON-TARGET COMPRESSION TOURNAMENT**: a
   panel of matched conditional-prediction tasks, never a pseudo-joint
   scalar. One canonical R0 world and one immutable observation document per
   seed (64 slices: 48 inference prefix, 16 held-out suffix; 16/32/48
   curves descriptive); per-seed sha256 identity of world, observations, and
   held-out targets across models; deterministic analysis-only adapters with
   zero RNG; five shared targets (identity, outcome-under-same-do(action),
   context, partner-response, contact-response); model-exclusive channels
   published as scope extensions but never scored or pseudo-scored in the
   criterion; per-family mean held-out log predictive score on identical
   (x, y); paired D = S_V3 − S_V2 reported as a five-element Pareto vector
   (macro mean descriptive only); the fourteen bridge proofs run before any
   criterion seed.
3. **Margin**: the old 0.018566762350958 margin is retired (wrong target,
   population, denominator). New prospective margin per shared target:
   δ = log(1.02) = 0.01980262729617973 nats per delivered target token;
   requirement lower95[E(D_τ)] ≥ −δ for every family, no exceptions.
   V2 precision qualification first: V2-only public calibration block must
   show full bootstrap-interval width ≤ δ per family (10,000 deterministic
   replicates); a wider interval is an apparatus stop, never a margin
   enlargement.
4. **Calibration**: predictive-equivalence-class calibration is primary on
   the tournament population (class ECE ≤ 0.05, 95% class-set coverage
   ≥ 0.90, active-count ECE ≤ 0.05, load-bearing edge ECE ≤ 0.05; classes
   defined by max-norm ≤ 1e-10 identity of predictive signatures over the
   frozen query schedule); exact-program statistics mandatory but
   descriptive; per-target predictive ECE ≤ 0.05 with reliability, Brier,
   log score, stratified by developmental stratum and active-mode count;
   own-prior calibration fixture on half the bridge block; reweighted
   posterior diagnostic only.
5. **Standing results**: both ≥50% reductions, all nine ablation floors,
   stakes identity/effect, and the stress categories stand — not rerun.
   C-V36A/B/C survive unchanged; before reveal the evaluator commits the
   six-point compatibility attestation. Continuation is pre-authorized:
   a valid numeric noninferiority failure blocks only the "no material
   predictive loss" sentence, never gates 4–5 or the challenges. Status
   vocabulary as ruled.
6. **Seed-map addendum (recorded here and in the ledger)**: bridge + V2
   precision qualification 3680000:3683999; repaired tournament
   3684000:3689999; repair diagnosis reserve 3690000:3699999. All prior
   bars unchanged.
7. **Do-over family (rulings 6–9)**: V3.3 stands as sealed with narrowed
   scope (equivalence under a rapidly saturating corrective stream; it does
   not establish that do-overs cannot accelerate under nonsaturation). The
   three findings are presented as one no-privileged-operation claim family
   with three separately-scoped instances, never pooled. The
   suggestion-direction anomaly gets an exact semantic enumeration (root and
   burden-edge log BF of the suggestion packet) BEFORE the timing extension
   is sealed: candidate-common ⇒ any nonzero direction is an apparatus stop;
   indirectly informative ⇒ freeze the analytically predicted sign and
   retire the verbal expectation. The sealed extension T-V3-DO1 is
   specified in full (frozen V3.6 model; seed namespace 5000000:5019999 +
   escrow 5100000:5105999; apparatus-only pacing qualification with frozen
   lexicographic grid and stop-on-unattainable; baseline-derived event
   anchor t_R^0 with premature/immediate/delayed offsets −4/+1/+8;
   evidence-matched arms including the bitwise imaginal-vs-ordinary
   identity; seven estimands; frozen ROPEs; the published interpretation
   grid). Post-ladder work; not required for V3.6 closure.
8. **Sequencing**: (1) this adjudication committed; (2) bridge built and
   frozen with repair analysis plan; (3) V3.6-R1 run once — apparatus
   failure returns to evaluator, a valid scientific result is retained
   either way; (4) gates 4–5 regardless, with the added bridge/calibration
   reruns; (5) compatibility attestation, then reveal/run the three
   challenges; (6) final V3 profile including every stop and both
   tournaments; (7) T-V3-DO1; (8) paper/HTML propagation with the ruled
   sentence templates keyed to the V3.6-R1 outcome.
