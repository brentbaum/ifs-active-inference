# V3.6 gate-4 and Population-A custody adjudication

Evaluator: Fable. Date: 2026-08-02.

## Rulings

Both stops are upheld and retained. Both are runtime custody incidents of
the established class (executed-prefix ambiguity under multiprocessing
prefetch), not pre-block proof failures, and are adjudicated under the
standing bar-and-reallocate precedent (V3.6 stage-0; round-13 ratified
boundary). Flagged for external review at the next consultation.

1. **Gate 4.** The stop's localization is adopted: the runner's
   conditioned-support helper treated the scorer's retained
   zero-probability entries as a support mismatch, producing a non-finite
   `masked_channel_neutrality_error` sentinel that strict JSON custody
   correctly rejected. This is an apparatus defect in the gate-4 runner
   helper; no scientific module or lesion construct is implicated and no
   criterion was evaluated. Because the executed prefix of
   `3630000:3634999` cannot be established, the block is **permanently
   barred**. Replacement gate-4 block: `3702000:3706999`.
2. **Population A.** The interruption was the round-13 binding rule
   operating as designed. The partial ledger (119 records, hashed) is
   retained as evidence only. The executed prefix of `3692001:3693999`
   cannot be established; the block is **permanently barred**. Replacement
   Population-A block: `3707000:3708999`.
3. **Authorized repairs (apparatus only):** fix the conditioned-support
   helper to treat retained zero-probability entries as in-support (exact
   zeros are identities in this suite, never support absences), with a
   regression test on an enumerable dummy; and a new permanent custody
   precondition — **every worker row must be validated finite (no
   inf/nan) before serialization**; a non-finite value is itself an
   immediate honest stop with the offending row's provenance recorded, and
   the trace file must contain all rows persisted up to that point (no
   zero-byte ledgers: open, fsync, and hash incrementally as the first
   row arrives). No scientific module, criterion, floor, or definition
   changes; source hashes bitwise unchanged.
4. **Unchanged:** Population B's PASS stands; Population C block
   `3694001:3695999`; tournament block `3684000:3689999`; gate-5 block;
   all escrows; all bars.

## Authorized continuation (per Brent's directive 2026-08-02)

Full remaining sequence: Population A (replacement) → Population C
qualification → the single tournament → gate 4 (replacement block, repaired
helper) in parallel after A opens → gate 5 after the tournament → challenge
compatibility attestations → C-V36A/B/C reveal-and-run (one at a time) →
V3.6 freeze and final v3 profile → T-V3-DO1 per the round-11 specification.
Paper and HTML propagation are explicitly deferred. Honest stop at any
failure per the standing rules; the next custody incident of any class
stops the program for external adjudication.
