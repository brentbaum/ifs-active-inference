# V3.6-R1 Round-12 resumption custody stop

Status: **CUSTODY_FAILURE_PUBLIC_DUMMY_SCORING_SINKS_NOT_PERSISTED_BEFORE_POPULATION_B**.

After applying the evaluator-authorized block rescope, I ran
`python3 -m unittest tests.test_v36_round12 tests.test_v36_bridge`. Four tests
invoked deterministic public-dummy scoring inside serializing trace contexts,
but did not persist those sinks' events before the command printed its result.
No random seed was consumed. This nevertheless violates the governing rule
that every generation or scoring invocation must persist before any output,
with no exception for one-off smoke calls.

I did not recognize that violation before opening Population B. Population B
then consumed `3690001:3691999` once, ascending and gap-free, and did satisfy
persist-before-aggregation custody for all 1,999 records. Its as-run verdict is
retained unchanged: **FAIL_APPARATUS_STOP**, because context ECE was
`0.057465146695993466 > 0.05`. The evaluator must decide whether that result is
probative given the prior ordering violation.

No Population A seed, Population C seed, tournament seed, Gate-4 seed,
Gate-5 seed, diagnosis seed, barred seed, or escrow seed was opened after the
rescope. There will be no local repair, rerun, or interpretation. Work stops
here for evaluator adjudication.

