# External round-15 rulings (verbatim; relayed by Brent 2026-08-03)

[Recorded exactly as received. Summary of operative content follows the full text in the session record; the operative JSON at the end is authoritative.]

## 1. Classification: apparatus-first theorem-premise failure, not yet a scientific failure of the grammar

Formal Population-A verdict retained: FAIL — NATIVE STRUCTURAL CALIBRATION BLOCKS. Present scientific status: STOPPED_AT_POPULATION_A / PROVISIONAL_APPARATUS_THEOREM_PREMISE_FAILURE — FULL_PATH_GENERATOR_SCORER_IDENTITY UNPROVEN. Not yet evidence the V3 grammar is intrinsically miscalibrated: if H ~ p(H), X ~ p(X|H), q(H|X) = p(H|X), structural-event posteriors are calibrated under the complete prior-predictive population, and the posterior-conditional nulls already account for finite-sample deviation. Current proofs establish only the factorized dummy identity, posterior normalization, and active-count marginalization consistency — NOT that the complete 64-slice generator joint equals the scorer's complete prior predictive. Mandatory apparatus localization before scientific interpretation; thresholds/estimators/priors/criteria frozen; no automatic third block; audit ALL theorem premises. Candidate defect locations: (1) structure/parameter draws differ from the scorer prior; (2) full latent-path process differs from the scorer path measure; (3) canonical-to-V3 adapter semantics differ from generation; (4) scorer long-path marginalization wrong despite short-dummy parity; (5) truth labels or calibration events misconstructed; (6) long-history numerical behavior.

Final classification after diagnosis — only two permitted outcomes: a proven concrete joint-measure mismatch → FAIL_APPARATUS_FULL_PATH_NATIVE_POPULATION_MISMATCH (conditional repair per ruling 3); all premises pass → FAIL_SCIENTIFIC_NATIVE_STRUCTURAL_CALIBRATION_QUALIFICATION with NO threshold adjustment or replacement sample (a valid qualification failure on the frozen population and estimators — not "proof that Bayesian V3 is inherently uncalibrated").

## 2. Diagnosis design: authorized, expanded, apparatus-first — five layers, committed before any diagnosis seed

LAYER A — read-only audit of the retained 2,000 worlds (no production rescoring, no new RNG):
A1 structure-draw audit vs the exact frozen prior (complete program frequency, active-count frequency, every edge, cross-sign, partner state, contact-response state, pairwise dependencies) — deterministic multinomial parametric test (observed multinomial log probability or Pearson statistic; null simulated from the exact frozen prior with a diagnosis-only RNG seed; Monte Carlo percentile; grouped marginals with simultaneous intervals). No KS on the 128-way categorical.
A2 complete-data log-joint identity per world from serialized truth/latents/interventions/masks/observations: log p_gen(H, latents, O) vs log p_score(H, latents, O), decomposed into structure prior, cross-sign prior, partner/contact priors, mode path, root/identity emission, joint-policy outcome emission, partner emission, contact-response emission, temporal-structure prior, context path, context emission, masking. Required: max abs full log-joint error <= 1e-10; max component recombination error <= 1e-10. If the required native path state was not serialized: FAIL_APPARATUS_REQUIRED_NATIVE_PATH_STATE_NOT_SERIALIZED — do NOT reconstruct from the seed.
A3 canonical-field semantic audit: commit a role per field (latent/observable/intervention/mask-availability/metadata); prove no latent modes_input is scored as an observed mode signal without an explicit generating emission; action/joint policy treated as do(...), never likelihood evidence; held-out targets unavailable at forecast time; masked context tokens contribute likelihood one; the adapter manufactures no observations absent from the native generator; every scored observation has exactly one generating factor. Factorization manifest, not just code inspection.
A4 truth/readout audit: independently reconstruct exact active count, JOINT_POLICY_Y existence, all load-bearing edge truths, and equivalence class from each truth program; independently reconstruct q(K=k) and q(Z_JPY=1) from each serialized posterior; confirm exact equality with the recorded calibration inputs.
A5 calibration-estimator replication: a second implementation of top-label ECE, macro classwise ECE, binary edge ECE, world weighting, bin assignment, class coverage — sharing only serialized inputs and the frozen definitions; exact agreement within 1e-12.

LAYER B — staged exact path enumeration on reduced grammars, T = 1..4 (5 only if tractable): fixtures covering active count 1v2 and 2v3, JOINT_POLICY_Y absent/present, cross-mode edge absent initially, partner/contact added progressively, the actual Population-A intervention schedule, root/outcome channels, masking, temporal/context last. Per length/fixture: generator joint normalizes; scorer prior predictive normalizes; identical support sets; joint-atom error <= 1e-10; exact posterior equality <= 1e-10; active-count and edge calibration exact under enumeration; complete-data and marginalized likelihoods recombine. Two genuinely independent implementations (production-semantics enumerator vs direct factorization oracle; shared type declarations only). Factor-addition ladder: structure prior → + latent mode paths → + root/identity emission → + do(policy)-conditioned outcome → + partner → + contact → + temporal/context → + masking; the first failing step localizes the mismatch.

LAYER C — numerical-stability audit on a representative retained subset (underconfident, overconfident, JPY-overpredicted, calibrated controls): independently authored high-precision log-domain recomputation (>= 80 decimal digits) of program log joints, log normalizer, posterior probabilities, active-count/edge/class posteriors; production vs high-precision error <= 1e-10; report count of programs whose production probabilities become exact zero. Mismatch = apparatus defect; match removes underflow.

LAYER D — diagnosis-only information-length ladder, ONLY if A–C do not localize: T=1 3716000:3716599; T=2 3716600:3717199; T=4 3717200:3717799; T=8 3717800:3718399; T=16 3718400:3718999; T=32 3719000:3719599; T=64 3719600:3720199; reserve 3720200:3720999. Each block diagnosis-only and barred after use; per length sample from the claimed complete native prior, serialize the complete path, compute the three failed statistics + their posterior-conditional nulls + percentiles, publish structure frequencies and complete-log identity. Divergence timing localizes (immediate = prior/edge semantics; multi-slice = path-transition/accumulated likelihood; long-length = numerical/path-measure accumulation). No floor or criterion from these blocks.

LAYER E — repair classification: GENERATOR_ONLY | BRIDGE_ADAPTER_ONLY | CALIBRATION_TRUTH_OR_ESTIMATOR_ONLY | FROZEN_SCIENTIFIC_SCORER_OR_PRIOR | NO_DEFECT_FOUND. Only the first three are eligible for the conditional replacement. A frozen-scorer/prior/code-length/grammar change stops for a new external round (could affect V3.5's closed results, C-V35B, V3.6 mechanisms, C-V36A/B/C compatibility).

## 3. Replacement boundary: one conditional Population-A replacement

Two earlier blocks were collateral custody burns; 3714000:3715999 was the first complete qualification execution and becomes an invalid apparatus run only if a concrete generator/adapter/truth/estimator mismatch is proven. Reserve Population A-R1 = 3722000:3723999, NOT opened until ALL committed: exact defect localization; apparatus-first repair statement independent of desired ECE direction; source-level diff; proof no frozen scientific model/likelihood/prior/threshold changed; staged path identity passes; complete-data log-joint identity passes; high-precision posterior identity passes; all pre-block fixture proofs pass; repair-world outputs differ only where the identified defect requires; seed-map and custody manifests updated. The failed block stays published, never pooled. ONE-replacement stop rule: A-R1 is the final Population-A block; if it fails a blocking criterion the result stands, no A-R2, Population C and the tournament stay unopened, return for closure adjudication. No-defect outcome: existing result stands as scientific qualification failure; do not open 3722000. Scientific-code outcome: return before any repair.

## 4. Sequencing and gate-4 proof

Everything stays closed during diagnosis (Population C, tournament, gate-4 seeded block, gate 5, escrows); only ruling-2 allocations usable; Population B's PASS stands. The recorded gate-4 zero-seed proof survives only a repair confined to the Population-A native generator / canonical bridge adapter / truth extraction / calibration estimator; if the V3.5/V3.6 scorer, structure prior, posterior aggregation, edge posterior construction, shared masking semantics, or shared numerical utilities change, rerun the complete gate-4 proof before its block. Gate-4 block remains 3709000:3713999 unopened. Resumption after a valid apparatus repair and A-R1 PASS: Population C → tournament → gate 4 → gate 5 → attestations → C-V36A, B, C → freeze → final profile → T-V3-DO1 → paper/HTML. Round-14 serial rule continues (one seeded battery at a time; first cell single-process).

## Operative authorization

{"round": 15, "population_a_formal_verdict_retained": "FAIL", "current_classification": "PROVISIONAL_APPARATUS_THEOREM_PREMISE_FAILURE", "scientific_interpretation_withheld": true, "threshold_changes_authorized": false, "diagnosis_authorized": {"retained_trace_prior_audit": true, "complete_data_log_joint_identity": true, "canonical_field_semantic_audit": true, "truth_and_readout_audit": true, "independent_calibration_estimator": true, "staged_exact_path_enumeration": [1, 2, 3, 4], "high_precision_posterior_audit": true, "diagnosis_length_ladder": true}, "diagnosis_seed_block": [3716000, 3720999], "conditional_population_a_replacement": [3722000, 3723999], "replacement_conditions": ["concrete apparatus mismatch proven", "scientific model and prior unchanged", "full-path identity passes", "all pre-block proofs pass"], "no_defect_found": "POPULATION_A_FAILURE_STANDS_NO_REPLACEMENT", "scientific_scorer_change_needed": "RETURN_TO_EXTERNAL_ADJUDICATION", "population_c_open": false, "tournament_open": false, "gate4_seeded_open": false, "gate5_open": false, "escrows_open": false, "gate4_preblock_proof_survives_generator_only_repair": true, "serial_remaining_sequence": true}
