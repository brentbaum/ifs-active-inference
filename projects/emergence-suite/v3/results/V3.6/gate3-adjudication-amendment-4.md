# V3.6 adjudication — amendment 4 (external round-12 rulings adopted)

Evaluator: Fable. Date: 2026-08-01. Source: external advisor round-12
rulings, relayed verbatim by Brent and recorded in
`external-round12-rulings.md`. Adopted as binding in full; where they
conflict with amendment 3 or any earlier record, round 12 governs.

Summary of what changes:

1. The R1 bridge stop is reclassified
   `STOPPED_AT_BRIDGE_QUALIFICATION_APPARATUS — NATIVE-POPULATION
   MISDEFINITION — FORECAST-SEMANTICS UNRESOLVED — CALIBRATION-ESTIMAND
   DEFECT`. Nothing about it bears on noninferiority. The 4,000-world
   results are retained but are never to be called own-prior calibration.
2. Three populations with separate semantics: A (V3 complete native prior
   predictive — blocking theorem-level battery), B (per-target V2 module
   native prior predictives — blocking; a correctly-defined native failure
   is a stop, not shift), C (external canonical tournament population —
   descriptive, non-blocking). Margin retained at log(1.02); V2 precision
   re-qualified on the new canonical population.
3. The hybrid generator is demoted to `HYBRID_GENERATOR_DIAGNOSIS_ONLY`.
   A new external shared-observable-support generator is required: declared
   Markov partner process, public context emission process, shared
   do(action) schedule, intersection support, external public parameter
   grid with the frozen lexicographic diagnosticity rule
   (0.20/0.50/0.80), balanced four-stratum population, no clinical labels
   in inference, one serialized document. `shared-target-support-audit.json`
   committed before any seed. Tournament population 6,000 balanced worlds.
4. Permanent bridge proof 15 — forecast-semantics identity for ALL five
   adapters at 1e-10 on enumerable dummies: every adapter must forecast the
   observable token's posterior predictive (marginalizing latents and
   transitions), never a latent posterior. Resolution order fixed
   (audit → adapter-only repair → hash identity → enumeration → native
   calibration → external qualification).
5. Calibration definitions frozen: world-weighting (1/N_w), ten fixed bins,
   binary target ECE, active-count top-label ECE + macro classwise ECE
   (never truth-mass × argmax), equivalence-class top-label ECE with
   deterministic HPD coverage, edge ECE, and the serialization rule with
   `FAIL_UNEXECUTABLE` for any preregistered quantity not recomputable from
   the persisted trace. The fixed-stratum equivalence-posterior omission is
   retained as an apparatus failure of the first R1 record.
6. Seeds: 3680000:3683999 barred; addendum 3690000:3691999 (V2 per-target
   native), 3692000:3693999 (V3 native), 3694000:3695999 (external
   generator qualification + adapter repetition + V2 precision +
   descriptive external calibration), 3696000:3699999 diagnosis only;
   tournament 3684000:3689999 reserved for the single run. ONE
   requalification cycle only.
7. Sequencing: Gate 4 authorized in parallel once the six pre-commitments
   land; Gate 5 waits for R1; challenge sequence unchanged, with the
   four-point non-import attestation added; final status vocabulary as
   ruled.

The seed-map addendum is recorded in `protocols/epoch-c-seed-map.json` by
this amendment.
