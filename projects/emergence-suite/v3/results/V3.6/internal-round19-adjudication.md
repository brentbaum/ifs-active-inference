# V3.6 Round-19 adjudication (INTERNAL)

**Authority.** Evaluator (Fable) as advisor per Brent's 2026-08-03
instruction; INTERNAL labeling, open to retroactive external review.

## Subject: round-18 diagnosis classifications

Both gate-4 failures are classified APPARATUS with named factors, and both
classifications are accepted:

- **D1 (protect_joint_policy) = ORACLE_CONSTRUCT.** The oracle keyed its
  comparison dictionary by `(structure, cross_sign)` while the scorer's
  atoms are `(structure, cross_sign, reliable)`; the reliable=1 atom
  overwrote reliable=0. Fresh coordinate-complete triangulation matches the
  production to 3e-17. Same defect family as the round-13 typed-channel
  misread; the permanent no-positional-access rule is hereby extended to
  DICTIONARY KEYS: every verifier-side keyed comparison must assert key-set
  equality with the scorer's full atom coordinates before comparing values.
- **D2 (split_context_slot) = APPARATUS_SUPPORT_ACCOUNTING_ERROR.** Zero
  licensed structures lose mass (all 144 positive at exactly 4/7); the
  support-positivity test exponentiated `restricted.log_evidence`, which
  underflows at 48-slice evidence scales, and read underflow as empty
  support. The 288 excluded structures are grammar-forced exclusions,
  exactly as the lesion intends. The finite-guard family is extended: NO
  support/positivity/emptiness predicate may be computed in linear space
  from a log-space quantity; such predicates must compare in log space.

Under pre-declared ruling 18.2, both cells are apparatus-classified, so the
original gate-4 FAIL remains in the ledger as a retained apparatus stop and
a full replacement gate-4 run is authorized.

## Ruling 19.1 — verifier-side repairs

Authorized, verifier/apparatus-side only: (a) oracle keys completed to the
full atom coordinate set with an asserted key-set equality; (b) the
licensed-support positivity predicate moved to log space. Differential audit
required; every frozen scientific-module hash must be bitwise unchanged
(the freeze manifest's 278-file set is the baseline). The two new permanent
rules (key-set equality; log-space predicates) join the standing defense
battery and apply to all future batteries including T-V3-DO1.

## Ruling 19.2 — replacement gate 4

Block `3728000:3732999` (5 × 1,000, per-cell serial first rows, zero-seed
lesion pre-run proofs re-run under the repaired verifier immediately before
opening, full custody standard). One-shot boundary: a scientific result on
the correctly instrumented battery STANDS either way; only a further
apparatus assertion returns here.

## Ruling 19.3 — gate-5 derivative verdict

Gate 5's own battery passed and its block is consumed. On a replacement
gate-4 PASS, the gate-5 cumulative verdict is RECOMPUTED FROM RETAINED
RECORDS ONLY (no new seeds, no rerun) and recorded as an amendment record
alongside the original derivative FAIL (both retained). On a replacement
gate-4 FAIL, gate 5's derivative FAIL stands unchanged.

## Ruling 19.4 — freeze and reveals

On replacement gate-4 PASS + gate-5 recompute: the V3.6 freeze manifest is
regenerated over the repaired verifier set and the freeze is DECLARED; the
evaluator then proceeds to the sealed reveals (C-V36A → B → C, one-shot
each) per the standing order. On any other outcome, return to adjudication
before the freeze.
