# V3.6-R1 Round-17 adjudication (INTERNAL)

**Authority.** Evaluator (Fable) acting in the advisor role under Brent's
2026-08-03 instruction; same INTERNAL labeling and retroactive-review
openness as round 16.

## Subject: the one-shot tournament verdict

`V3.6_COMPRESSION_PREDICTIVE_COST_RETAINED` — v3 fails the compression
criterion on four of five target families (identity, outcome, partner,
contact) and passes decisively on context. The verdict is immutable, custody
is clean (6,000 worlds, byte-identical adapter documents, fresh coherence
proof, hash ledgers reverified, no weighted aggregate), and under the
one-shot boundary it STANDS. No rerun, re-analysis, or criterion amendment
is authorized or will be.

## Ruling 17.1 — scientific reading (descriptive, retained with the verdict)

The per-stratum decomposition supports a coherent reading:

- **Context (+0.269 overall)**: v3 wins by ~0.34–0.35 nats/token in all
  three chronic/recurrent strata and is ~flat (+0.03) in acute_one. This is
  the organism's structural specialty — context-indexed redescription — and
  the gain is largest exactly where contexts recur.
- **Partner (−0.294) and contact (−0.240)**: losses are *uniform across all
  four strata* (spread < 0.03 nats). Uniformity across strata that differ in
  everything structural indicates a modeling-capacity gap, not a
  world-structure interaction: v2 carries the round-10 per-target modules
  (hierarchical partner process, θ_contact re-parameterization) that the
  minimal organism deliberately lacks.
- **Identity (−0.180)**: concentrated in acute_one (−0.249) and
  real_danger_adaptive (−0.297), mild in chronic_one (−0.054) — v3's
  identity posterior is weakest where mode activity is transient or where
  danger is real rather than mode-generated.
- **Outcome (−0.021)**: driven almost entirely by acute_one (−0.096); the
  other three strata are ≈ 0 (+0.010, +0.009, −0.006). The family fails the
  frozen criterion, and that stands; descriptively the deficit is localized,
  not general.

One-sentence retained interpretation: **structure is not free** — the
minimal structural organism converts a large, targeted gain on
context-indexed prediction into broad per-token costs on the social channels
it deliberately compressed, and the tournament quantifies that exchange rate.

The verdict string's `WITH_RETAINED_R1_BRIDGE_QUALIFICATION_FAILURE` suffix
is the retained historical annotation of the original hybrid-generator
qualification failure (superseded by the round-12 three-population
semantics); it is not a new failure.

## Ruling 17.2 — continuation

The chain continues unchanged. The tournament is one retained line of the
final v3 profile, not a gate over the remaining items: gates 4–5 assess the
organism's internal validity (lesion identity and robustness), the
attestations and freeze close the mechanical record, and the sealed
challenges C-V36A/B/C test different claims (formation, protection,
temporal mixing) under their own sealed criteria. Order per rounds 14–16:
gate 4 (`3709000:3713999`, ruled lesion semantics, zero-seed pre-run proofs
already PASS) → gate 5 (`3635000:3659999`) → attestations → V3.6 freeze →
evaluator reveals A/B/C → final v3 profile (tournament FAIL retained
verbatim) → T-V3-DO1.

## Ruling 17.3 — prohibition on post-hoc softening

The final profile and any later paper text may carry the descriptive
decomposition of 17.1 alongside the verdict, but may not report a weighted
aggregate, may not re-score any family under alternative deltas, and may not
describe the four failing families as "near-miss" (outcome's CI upper bound
−0.018 is within 0.002 of the margin; it still fails; both facts are stated,
neither is softened).
