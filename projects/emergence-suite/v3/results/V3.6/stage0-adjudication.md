# V3.6 stage-0 adjudication — premature-do-over declaration defect

Evaluator: Fable. Date: 2026-07-31.

## Standing of the stop

The honest stop is upheld and retained. The pilot block `3600001:3603999`
was consumed once with full persisted custody (the reinforced rule held) and
is barred. No floor was frozen; gates and all three escrows are untouched.

## Ruling: the failed criterion declared the wrong construct

The analysis plan (line 55) declared "full versus premature-do-over current
edge absence" as an EQUIVALENCE. That declaration contradicts the suite's own
sealed V3.3 record, which distinguishes the two do-over variants explicitly:

- **Post-revision do-over** is the equivalence (the sealed C-V33 null: after
  configural revision, an enacted do-over adds nothing — the
  no-privileged-operation prediction). The plan carries this correctly at
  line 65 as a retained finding.
- **Premature do-over** (enacted before the material revision boundary) was
  sealed as a SPECIFICITY result: premature+return produced exactly 0.0
  durable reduction. Cross-suite consistency therefore predicts the composed
  full-minus-premature edge-absence difference to be POSITIVE — the full
  trajectory achieves reduction the premature shortcut does not hold.

The pilot behaved exactly as V3.3 predicts: mean `+0.0151` in the full-favors
direction, with the other nine contrasts passing and the stakes identity
exact. Declaring equivalence for the premature variant was a plan-fidelity
defect adjudicable entirely from the retained V3.3 record; no pilot value is
used to select any numeric strength.

This is also a process finding worth keeping visible: a composition stage
must inherit each constituent stage's sealed CLAIM STRUCTURE, not merely its
vocabulary. The failure record retains it.

## Required repair

1. Re-declare the estimand: "full minus premature-do-over
   `q_current_edge_absence`" — direction POSITIVE, kind causal effect.
   The post-revision equivalence at line 65 is unchanged.
2. Premature scheduling must be event-indexed (the V3.3 lesson): premature
   means before the world's material revision boundary as experienced, never
   a fixed slice index. Verify the composed schedule satisfies this with a
   regression test; if the composed generator schedules by fixed slice,
   correct it as plan fidelity.
3. Same-seed pairing and shared potential-outcome streams as everywhere else.

## Seed authorization

- Barred: `3600001:3603999` (consumed pilot), `3600000` (prior custody bar).
- Fresh attainability pilot: `3660000:3663999` from the V3.6
  diagnosis-reserved namespace, exactly once, persisted ledgers.
  All ten contrasts re-pilot on this block; every floor freezes from it
  mechanically under the unchanged rule. Attainability for the corrected
  premature contrast requires its paired 95% interval to carry the positive
  sign; if it does not, honest stop — the result would then be a genuine
  composition finding, and it returns here unrepaired.
- Diagnosis remainder `3664000:3679999` stays diagnosis-only.
- Gates 2–5 blocks unchanged and unopened; C-V36A/B/C escrows untouched.

One repair cycle is authorized. Any further stage-0 failure returns to the
evaluator before seed consumption.
