# V3.6 fresh pilot — honest stop

Verdict: **FAIL — genuine composition finding**.

The adjudicated pilot `3660000:3663999` was run exactly once. All 4,000 seeds
were consumed ascending and gap-free. Runtime events and per-world records were
persisted to JSONL, the record and whole-file hash ledger was written, and the
on-disk hash was verified before aggregation or output. No barred, remaining
diagnosis, Gate 2--5, or escrow seed was touched.

The event-indexed schedule was active on every world: premature imagery was
audited relative to the root-revision event observed in that world, never a
fixed slice. The moving-boundary regression fixture passed at boundaries 7 and
19.

The corrected preregistered estimand was

`q_current_edge_absence(full) - q_current_edge_absence(premature_do_over)`.

Its mean was `-0.007591287369016907`; its paired whole-world bootstrap 95%
interval was `[-0.03688060730166053, 0.021721859932301718]`. The interval does
not carry the declared positive sign. Under `stage0-adjudication.md`, this is a
genuine composition finding and ends the authorized repair cycle.

For localization only, the other nine positive comparator contrasts passed,
the stakes scientific-posterior identity remained exact (`0.0`), and the
stakes policy contrast remained positive (`95% interval [0.10291542939176537,
0.10598819209407398]`). These results do not rescue the failed construct.

No floor was frozen and no freeze-readiness or manifest was issued. V3.6 stops
for evaluator adjudication; no further repair is self-authorized.
