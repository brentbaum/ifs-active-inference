# V3.6-R1 round-12 precommit custody stop

Disposition:
**CUSTODY_FAILURE_PRECOMMIT_SMOKE_CONSUMED_ASSIGNED_SEEDS_WITHOUT_PERSISTED_LEDGERS_AND_GIT_METADATA_READ_ONLY**.

The Phase-1 apparatus is complete. Permanent proof 15 passes for all five
targets and both adapters with maximum error `0.0`; shared support passes; all
frozen scientific source hashes match; the 17-file precommit manifest verifies
17/17; and the V3 suite passes 70/70.

The binding order requires these artifacts to be committed before seed
`3690000` or any Gate-4 seed is opened. During precommit development, three
assigned first seeds were mistakenly invoked as smoke checks:

- `3690000`: five Population-B target-native fixture calls;
- `3692000`: one Population-A native world and calibration-state call;
- `3694000`: one Population-C external world call.

Each ran inside an in-memory trace context, but no event ledger or per-world
record was persisted and hashed. No criterion was evaluated. These calls mean
the three qualification blocks can no longer be consumed once, ascending and
gap-free as authorized without evaluator adjudication.

Separately, the commit attempt failed before any file was staged because this
execution environment cannot create `.git/index.lock`:

```text
fatal: Unable to create '/Users/brentbaum/dev/research/ifs-active-inference/.git/index.lock': Operation not permitted
```

No other qualification seed and no tournament, Gate-4, Gate-5, diagnosis,
barred, or escrow seed was touched. Work stops for evaluator custody
adjudication; merely committing the Phase-1 package is not sufficient because
the three premature smoke calls must also be resolved.
