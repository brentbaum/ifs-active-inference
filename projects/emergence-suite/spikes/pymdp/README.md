# pymdp spike

Runnable Python/pymdp spike for T0.1. The script uses `uv` inline-script dependencies and imports `pymdp.Agent`; the cross-trial Dirichlet learning/count resizing is explicit so the dynamic-growth behavior is inspectable.

Run:

```sh
uv run projects/emergence-suite/spikes/pymdp/spike.py
```
