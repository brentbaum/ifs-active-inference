# v10_port spike

Runnable Julia port of the v10-style discrete active-inference core for T0.1.

Run:

```sh
PATH="$HOME/.juliaup/bin:$PATH" julia projects/emergence-suite/spikes/v10_port/spike.jl
```

If the Julia launcher needs to update its depot lockfile, run the same command with the normal user environment enabled.
