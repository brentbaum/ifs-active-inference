# E5 pre-run interpretation lock

Locked before the E5 released block was run.

Post-run apparatus disposition C-002: the intended depth/broadcast operationalization required runner-authored coordinates rather than an endogenous frozen-organism readout. It is retained as a record of the invalid attempt, not as a scientific analysis lock; see `stage-c-errata.md`.

- The frozen grammar expressly represents `part_slots = 1`, `local_monitor`, and `recursive_broadcast_off`; E5 therefore proceeds rather than taking its inexpressibility branch.
- A separate local state receives the part-slot’s own forecast error through `update_precision_field!(...; broadcast=false)`. In the self-like arm that local state is not copied into the global state, so its forecasts do not enter global q(Φ). The interoception observation is omitted from that arm rather than assigned a value.
- Local confidence is the part channel’s canonical precision; calibration is `1 -` mean absolute error against the canonical instantaneous target `exp(-part_local_error)`.
- Assay 3’s two coordinates are applied conservatively. Part dominance is local confidence relative to the mean global field confidence. Depth is the fraction of the five global field channels receiving the part’s recursively shared forecast (zero without broadcast, one with full broadcast). The frozen assay 3 threshold of `0.5` classifies the resulting regime.
- “Context-held witnessing” is assay 3’s low-dominance/high-depth `self_led_witnessing` cell. The E5 self-like configuration must not enter that cell.
- Policy-priority change is the mean absolute change in the four frozen expected policy costs. In the full-broadcast arm witnessed policy-cost/success observations enter the ordinary update path; without broadcast they remain local and do not update the global repertoire.
- Root revision uses the frozen root probability change. Full broadcast supplies its resulting relational-field breadth to `update_root!`; no-broadcast supplies zero global breadth. No posterior endpoint is authored.
