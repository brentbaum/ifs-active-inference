# E4 pre-run interpretation lock

Locked before the E4 released block was run.

Post-run apparatus disposition C-002: this intended operationalization could not provide the protocol’s common delivered-log-likelihood audit through the frozen organism interface. It is retained as a record of the invalid attempt, not as a scientific analysis lock; see `stage-c-errata.md`.

- Root revision is the frozen assay 4 probability change, with revision present at `root_revision_begin`; its full probability scale is `[0,1]`, so the protocol’s explicit `0.10` margin applies.
- A counter-episode carries the four co-occurring self/world/policy/outcome feature observations as one event. The cue arm receives the identical four marginals as four isolated events and never presents their conjunction.
- Corrective log-likelihood budget is audited at the input: the sum of absolute Bernoulli log-likelihood contributions across the four features. This is identical by construction across paired arms but is still computed and checked world by world.
- Treated-cue learning uses assay 4’s frozen `cue_positive_step`/`cue_negative_step`. Both arms receive identical treated-cue marginals; only the configural arm routes the jointly configured event to the shared root through `update_root!`.
- Untreated-cue transfer uses assay 4’s frozen `cue_transfer_weight` mapping from root movement. “Present” means at least `assay4_transfer_margin`; “absent” means below that margin.
